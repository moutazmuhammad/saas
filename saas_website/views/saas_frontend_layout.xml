<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================
         White-label: replace the `<meta name="generator" content="Odoo">`
         that website.layout injects on every QWeb-rendered page (checkout,
         /my/invoices, etc.) so the source no longer fingerprints the
         underlying stack. The SPA shell at `/` and other `spa_shell()`
         routes never include this meta — they ship a plain HTML doc — so
         this only affects the legacy Odoo-rendered pages.
         ============================================================ -->

    <template id="veltnex_hide_generator" name="VELTNEX: Hide Generator"
              inherit_id="website.layout" customize_show="False">
        <xpath expr="//meta[@name='generator']" position="attributes">
            <attribute name="content">VELTNEX</attribute>
        </xpath>
    </template>

    <!-- ============================================================
         Inject early theme script to prevent FOUC (flash of wrong theme).
         Runs synchronously before CSS paints.
         ============================================================ -->

    <!-- ============================================================
         SERVER-SIDE theme injection.
         Reads the `veltnex-theme` cookie (set by both the SPA's
         useTheme hook and the QWeb veltnex_theme.js) and stamps the
         <html> tag with the matching `data-theme` attribute and
         (optionally) the `.dark` class. The CSS in cloudodoo.css
         keys on `data-theme` so this gives the correct palette from
         the first byte of HTML — no FOUC, no JS race.
         ============================================================ -->

    <template id="cloudodoo_html_theme" name="VELTNEX: HTML data-theme"
              inherit_id="website.layout" customize_show="False" priority="100">
        <!--
            Inject `data-theme` (and matching `dark` class) into the
            html_data dict that website.layout spreads onto <html>.
            This is more robust than `t-att-data-theme` on the same
            element because we don't fight the existing `t-att`
            spread — we just add a key to the dict it spreads.
        -->
        <xpath expr="//html" position="before">
            <t t-set="_vx_theme"
               t-value="request.httprequest.cookies.get('veltnex-theme') if request.httprequest.cookies.get('veltnex-theme') in ('light', 'dark') else 'dark'"/>
            <t t-set="html_data" t-value="dict(html_data or {}, **{
                'data-theme': _vx_theme,
                'class': ((html_data or {}).get('class', '') + (' dark' if _vx_theme == 'dark' else '')).strip(),
            })"/>
        </xpath>
    </template>

    <template id="cloudodoo_theme_init" name="CloudOdoo Theme Init"
              inherit_id="web.frontend_layout" customize_show="False">
        <xpath expr="//head" position="inside">
            <script>
                (function(){
                    // 1) Apply theme BEFORE paint (mirrors the SPA's
                    //    inline script in veltnex/index.html so the
                    //    same localStorage key drives both worlds).
                    function apply(t) {
                        document.documentElement.setAttribute('data-theme', t);
                        document.documentElement.classList.toggle('dark', t === 'dark');
                    }
                    var t;
                    try {
                        // Trust the server-rendered `data-theme`
                        // (cloudodoo_html_theme sets it from the
                        // cookie). Don't re-decide — just sync
                        // localStorage + cookie to match. This is
                        // what eliminates the white→dark flash on
                        // navigation: nothing flips the attribute
                        // after the server has already set it.
                        var server = document.documentElement.getAttribute('data-theme');
                        if (server === 'light' || server === 'dark') {
                            try { localStorage.setItem('veltnex-theme', server); } catch (err) {}
                            document.cookie = 'veltnex-theme=' + server + '; path=/; max-age=31536000; SameSite=Lax';
                            t = server;
                        } else {
                            t = localStorage.getItem('veltnex-theme');
                            if (t !== 'light' &amp;&amp; t !== 'dark') {
                                t = (window.matchMedia &amp;&amp; window.matchMedia('(prefers-color-scheme: light)').matches)
                                    ? 'light' : 'dark';
                            }
                            apply(t);
                            try { localStorage.setItem('veltnex-theme', t); } catch (err) {}
                            document.cookie = 'veltnex-theme=' + t + '; path=/; max-age=31536000; SameSite=Lax';
                        }
                    } catch (e) { apply('dark'); }

                    // 2) Live sync — if the user toggles theme in
                    //    another tab (SPA toggle writes localStorage),
                    //    repaint THIS page without a reload.
                    window.addEventListener('storage', function(e){
                        if (e.key === 'veltnex-theme' &amp;&amp; (e.newValue === 'light' || e.newValue === 'dark')) {
                            apply(e.newValue);
                        }
                    });

                    // 3) Re-sync on visibility change (catches the
                    //    case where another tab changed it while we
                    //    were backgrounded — storage event doesn't
                    //    always fire for backgrounded tabs).
                    document.addEventListener('visibilitychange', function(){
                        if (!document.hidden) {
                            try {
                                var v = localStorage.getItem('veltnex-theme');
                                if ((v === 'light' || v === 'dark') &amp;&amp;
                                    v !== document.documentElement.getAttribute('data-theme')) {
                                    apply(v);
                                }
                            } catch (e) {}
                        }
                    });

                    // 4) Scroll-to-blur on the navbar to match the SPA
                    //    PublicNav's transparent-at-top behaviour. The
                    //    selector covers both the cloudodoo navbar
                    //    (`.co-navbar`, on website.layout pages) and
                    //    the default portal navbar (`navbar-light
                    //    bg-light`, on /payment/* and /my/invoices/*),
                    //    which we restyle via CSS to look the same.
                    document.addEventListener('DOMContentLoaded', function(){
                        var nav = document.querySelector('.co-navbar, nav.navbar.navbar-light.bg-light');
                        if (!nav) return;
                        var onScroll = function(){
                            nav.classList.toggle('co-navbar--scrolled', window.scrollY > 8);
                        };
                        window.addEventListener('scroll', onScroll, { passive: true });
                        onScroll();
                    });
                })();
            </script>
            <link rel="icon" type="image/svg+xml" href="/saas_website/static/src/img/veltnex-icon.svg"/>
            <link rel="shortcut icon" href="/saas_website/static/src/img/veltnex-icon.svg"/>
            <link rel="preload" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" as="style"/>
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"/>
        </xpath>
    </template>

    <!-- ============================================================
         Override Odoo's website header with VELTNEX navbar.
         The full markup is inlined (rather than extracted to a shared
         t-call template) so any future descendant inherits can still
         anchor on <header>/<nav> elements that exist directly in
         this view's arch — Odoo 18's view validator only sees the
         immediate parent's bare arch when resolving xpaths.
         ============================================================ -->

    <template id="cloudodoo_header" name="CloudOdoo Header"
              inherit_id="website.layout" customize_show="False">
        <xpath expr="//header" position="replace">
            <t t-set="show_services_section"
               t-value="request.env['ir.config_parameter'].sudo().get_param('saas_master.show_services_section', 'True') != 'False'"/>
            <t t-set="show_hosting_section"
               t-value="request.env['ir.config_parameter'].sudo().get_param('saas_master.show_hosting_section', 'True') != 'False'"/>
            <header id="top" data-anchor="true">
                <nav class="navbar navbar-expand-lg fixed-top co-navbar">
                    <div class="container">
                        <a class="navbar-brand d-flex align-items-center" href="/">
                            <img src="/saas_website/static/src/img/veltnex-icon.svg" alt="VELTNEX" style="height:30px;" class="me-2"/>
                            <span class="fw-bold">VELT<span style="color:#3656b8;">NEX</span></span>
                        </a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#coNavbar">
                            <span class="navbar-toggler-icon"></span>
                        </button>
                        <div class="collapse navbar-collapse" id="coNavbar">
                            <ul class="navbar-nav me-auto align-items-center">
                                <li t-if="show_services_section" class="nav-item">
                                    <a class="nav-link" href="/services">Services</a>
                                </li>
                                <li t-if="show_hosting_section" class="nav-item">
                                    <a class="nav-link" href="/hosting">Hosting</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="/docs">Docs</a>
                                </li>
                            </ul>
                            <ul class="navbar-nav align-items-center">
                                <li class="nav-item me-2">
                                    <!-- Click handler wired via event delegation in
                                         veltnex_theme.js — no inline onclick so the
                                         page passes a `script-src` CSP without
                                         `unsafe-inline`. -->
                                    <button type="button" class="vx-theme-toggle vx-theme-toggle-nav"
                                            aria-label="Toggle theme" title="Toggle theme">
                                        <!-- Inline lucide icons to match the SPA's ThemeToggle 1:1
                                             (FontAwesome's fa-sun reads as a cog at this size). -->
                                        <svg class="vx-icon-sun" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4"/><path d="M12 2v2"/><path d="M12 20v2"/><path d="m4.93 4.93 1.41 1.41"/><path d="m17.66 17.66 1.41 1.41"/><path d="M2 12h2"/><path d="M20 12h2"/><path d="m6.34 17.66-1.41 1.41"/><path d="m19.07 4.93-1.41 1.41"/></svg>
                                        <svg class="vx-icon-moon" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"/></svg>
                                    </button>
                                </li>
                                <t t-if="not request.env.user._is_public()">
                                    <li class="nav-item dropdown ms-lg-3">
                                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#"
                                           role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                            <div class="d-flex align-items-center justify-content-center rounded-circle me-2"
                                                 style="width:32px;height:32px;background:var(--co-primary, #203c86);font-size:0.8rem;color:white;">
                                                <t t-esc="''.join([w[0] for w in (request.env.user.name or 'U').split()[:2]]).upper() or 'U'"/>
                                            </div>
                                            <span class="d-none d-lg-inline" t-esc="request.env.user.name"/>
                                            <!-- lucide chevron-down, replacing Bootstrap's caret to
                                                 match the SPA UserMenu (rotates 180° when open). -->
                                            <svg class="vx-chevron" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m6 9 6 6 6-6"/></svg>
                                        </a>
                                        <ul class="dropdown-menu dropdown-menu-end">
                                            <li><a class="dropdown-item" href="/my"><i class="fas fa-th-large me-2"></i>Dashboard</a></li>
                                            <li><a class="dropdown-item" href="/my/account"><i class="fas fa-user me-2"></i>Profile</a></li>
                                            <li><a class="dropdown-item" href="/my/instances"><i class="fas fa-server me-2"></i>My Instances</a></li>
                                            <li t-if="request.env.user.has_group('base.group_user')">
                                                <a class="dropdown-item" href="/odoo"><i class="fas fa-cogs me-2"></i>Backend</a>
                                            </li>
                                            <li><hr class="dropdown-divider"/></li>
                                            <li><a class="dropdown-item text-danger" href="/web/session/logout?redirect=/"><i class="fas fa-sign-out-alt me-2"></i>Sign Out</a></li>
                                        </ul>
                                    </li>
                                </t>
                                <t t-if="request.env.user._is_public()">
                                    <li class="nav-item ms-lg-3">
                                        <a class="nav-link" href="/login">Sign in</a>
                                    </li>
                                    <li class="nav-item ms-2">
                                        <a class="btn btn-primary btn-sm" href="/services/register">Get started</a>
                                    </li>
                                </t>
                            </ul>
                        </div>
                    </div>
                </nav>
            </header>
        </xpath>
    </template>

    <!-- ============================================================
         Payment / portal pages (/payment/*, /my/invoices/*, ...) use
         `portal.frontend_layout` directly, which renders its own
         `<nav class="navbar navbar-light bg-light">` inside the page
         header. Trying to replace that header via XML inheritance is
         brittle in Odoo 18 (neither `web.frontend_layout` nor
         `portal.frontend_layout` exposes the `<header>` element in
         its *own* arch — both define it through xpath ops on
         `web.layout`, so xpath inheritance validation can't anchor
         on it).
         Instead, the theme-init script in `cloudodoo_theme_init`
         rewrites the rendered DOM at page load to swap that default
         navbar/footer for the shared VELTNEX ones — see the JS in
         the head template for the implementation.
         ============================================================ -->

    <!-- Override Odoo's website footer with the VELTNEX footer (inline
         markup so descendant inherits can anchor on <footer> if needed). -->

    <template id="cloudodoo_footer" name="CloudOdoo Footer"
              inherit_id="website.layout" customize_show="False">
        <xpath expr="//footer" position="replace">
            <t t-set="show_services_section"
               t-value="request.env['ir.config_parameter'].sudo().get_param('saas_master.show_services_section', 'True') != 'False'"/>
            <t t-set="show_hosting_section"
               t-value="request.env['ir.config_parameter'].sudo().get_param('saas_master.show_hosting_section', 'True') != 'False'"/>
            <footer id="bottom" data-anchor="true" class="co-footer">
                <div class="container">
                    <div class="row g-4 py-5">
                        <div class="col-12 col-lg-3">
                            <a href="/" class="d-flex align-items-center mb-3 text-decoration-none">
                                <img src="/saas_website/static/src/img/veltnex-icon.svg" alt="VELTNEX" style="height:30px;" class="me-2"/>
                                <span class="fw-bold fs-5" style="color:var(--co-text-primary, #FAFAFA);">VELT<span style="color:#3656b8;">NEX</span></span>
                            </a>
                            <p class="small mb-4" style="color:var(--co-text-muted, #71717A);max-width:22rem;">The Logic of Stability. Enterprise-grade managed hosting for Odoo, built for teams that can't afford downtime.</p>
                            <div class="d-flex gap-2">
                                <a href="#" class="co-social" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
                                <a href="#" class="co-social" aria-label="GitHub"><i class="fab fa-github"></i></a>
                                <a href="#" class="co-social" aria-label="LinkedIn"><i class="fab fa-linkedin"></i></a>
                            </div>
                        </div>
                        <div class="col-6 col-lg-3">
                            <h6 class="fw-bold mb-3">Product</h6>
                            <ul class="list-unstyled">
                                <li t-if="show_services_section"><a href="/services" class="co-footer-link">Services</a></li>
                                <li t-if="show_hosting_section"><a href="/hosting" class="co-footer-link">Hosting</a></li>
                                <li t-if="show_hosting_section"><a href="/hosting/configure" class="co-footer-link">Configure</a></li>
                                <li><a href="/docs" class="co-footer-link">Docs</a></li>
                            </ul>
                        </div>
                        <div class="col-6 col-lg-3">
                            <h6 class="fw-bold mb-3">Company</h6>
                            <ul class="list-unstyled">
                                <li><a href="#" class="co-footer-link">About</a></li>
                                <li><a href="#" class="co-footer-link">Customers</a></li>
                                <li><a href="#" class="co-footer-link">Careers</a></li>
                                <li><a href="/contactus" class="co-footer-link">Contact</a></li>
                            </ul>
                        </div>
                        <div class="col-6 col-lg-3">
                            <h6 class="fw-bold mb-3">Account</h6>
                            <ul class="list-unstyled">
                                <li><a href="/login" class="co-footer-link">Sign in</a></li>
                                <li><a href="/services/register" class="co-footer-link">Get started</a></li>
                                <li><a href="/my" class="co-footer-link">Dashboard</a></li>
                                <li><a href="/my/billing" class="co-footer-link">Invoices</a></li>
                            </ul>
                        </div>
                    </div>
                    <div class="d-flex flex-column flex-sm-row justify-content-between align-items-center gap-2 py-4"
                         style="border-top:1px solid var(--border-color, #27272A);">
                        <p class="small mb-0" style="color:var(--co-text-muted, #71717A);">&#169; 2026 VELTNEX, Inc. All rights reserved.</p>
                        <p class="small mb-0" style="color:var(--co-text-muted, #71717A);">Status: <span style="color:#10B981;">All systems operational</span></p>
                    </div>
                </div>
            </footer>
        </xpath>
    </template>

    <!-- Portal/payment footer: same story as the navbar above —
         replaced at runtime via the head script, not via XML inheritance. -->

    <!-- ============================================================
         Remove Salesperson card from invoice portal page
         ============================================================ -->

    <template id="hide_invoice_salesperson" name="Hide Invoice Salesperson"
              inherit_id="account.portal_invoice_page" customize_show="False">
        <xpath expr="//div[@id='invoice_communication']" position="replace"/>
    </template>

    <!-- ============================================================
         Remove Salesperson + Communication from Sale Order page
         ============================================================ -->

    <template id="hide_so_salesperson" name="Hide SO Salesperson"
              inherit_id="sale.sale_order_portal_template" customize_show="False">
        <xpath expr="//div[@id='sale_order_communication']" position="replace"/>
    </template>

    <template id="hide_so_contact" name="Hide SO Contact"
              inherit_id="sale.sale_order_portal_template" customize_show="False">
        <xpath expr="//div[@t-if='sale_order.user_id']" position="replace"/>
    </template>

    <!-- ============================================================
         Remove Salesperson from invoice sidebar
         ============================================================ -->

    <template id="hide_invoice_contact" name="Hide Invoice Contact"
              inherit_id="account.portal_invoice_page" customize_show="False">
        <xpath expr="//div[@t-if='invoice.invoice_user_id']" position="replace"/>
    </template>

    <!-- ============================================================
         Remove "Powered by Odoo" everywhere — override the source
         templates to render nothing.
         ============================================================ -->

    <template id="hide_brand_message" name="Hide Brand Message"
              inherit_id="web.brand_promotion_message" customize_show="False">
        <xpath expr="//t[@t-set='odoo_logo']" position="replace"/>
        <xpath expr="//t[@t-set='final_message']" position="replace"/>
        <xpath expr="//t[@t-out]" position="replace"/>
    </template>

    <template id="hide_brand_promotion" name="Hide Brand Promotion"
              inherit_id="web.brand_promotion" customize_show="False">
        <xpath expr="//div[hasclass('o_brand_promotion')]" position="attributes">
            <attribute name="style">display:none !important;</attribute>
        </xpath>
    </template>

    <!-- The copyright bar is hidden via CSS instead of XML inheritance.
         The original xpath `//footer[@id='bottom']` can't be located in
         `web.frontend_layout`'s bare arch (the <footer> only exists in
         the xpath-replacement content, which Odoo 18's inheritance
         validator doesn't traverse). See cloudodoo.css → "Hide Odoo's
         default copyright bar on all QWeb pages". -->

    <!-- Remove "Powered by Odoo" from portal record sidebar
         (invoice detail, sale order detail, etc.) -->
    <template id="hide_portal_sidebar_branding" name="Hide Portal Sidebar Branding"
              inherit_id="portal.portal_record_sidebar" customize_show="False">
        <xpath expr="//div[hasclass('o_portal_sidebar_content')]/div[hasclass('text-muted')]" position="replace"/>
    </template>

    <!-- ============================================================
         Restyle portal side_content (user profile card on /my/home)
         ============================================================ -->

    <template id="cloudodoo_side_content" name="CloudOdoo Side Content"
              inherit_id="portal.side_content" customize_show="False">
        <!-- Replace the avatar + name block -->
        <xpath expr="//div[hasclass('d-flex','justify-content-start')]" position="replace">
            <div class="d-flex align-items-center gap-3 mb-3 pb-3" style="border-bottom:1px solid var(--border-color, #27272A);">
                <div class="d-flex align-items-center justify-content-center rounded-circle"
                     style="width:50px;height:50px;background:var(--co-primary, #203c86);color:white;font-size:1.1rem;font-weight:600;flex-shrink:0;">
                    <t t-esc="(user_id.name or 'U')[:2].upper()"/>
                </div>
                <div>
                    <h5 class="mb-0" style="color:var(--co-text-primary, #FAFAFA);" t-out="user_id.name"/>
                    <p t-if="user_id.company_name" class="mb-0 small" style="color:var(--co-text-muted, #71717A);" t-out="user_id.company_name"/>
                </div>
            </div>
        </xpath>
        <!-- Replace the contact details widget with icon-based layout -->
        <xpath expr="//div[hasclass('o_portal_my_details')]" position="replace">
            <div class="o_portal_my_details d-flex flex-column gap-2">
                <t t-set="partner" t-value="user_id.partner_id"/>
                <t t-if="partner.email">
                    <div class="d-flex align-items-center gap-2">
                        <i class="fas fa-envelope" style="width:18px;color:var(--co-text-muted, #71717A);font-size:0.85rem;"></i>
                        <a t-att-href="'mailto:' + partner.email" class="small text-break"
                           style="color:var(--co-primary, #203c86);" t-esc="partner.email"/>
                    </div>
                </t>
                <t t-if="partner.phone">
                    <div class="d-flex align-items-center gap-2">
                        <i class="fas fa-phone" style="width:18px;color:var(--co-text-muted, #71717A);font-size:0.85rem;"></i>
                        <span class="small" style="color:var(--co-text-secondary, #A1A1AA);" t-esc="partner.phone"/>
                    </div>
                </t>
                <t t-if="partner.city or partner.country_id">
                    <div class="d-flex align-items-center gap-2">
                        <i class="fas fa-map-marker-alt" style="width:18px;color:var(--co-text-muted, #71717A);font-size:0.85rem;"></i>
                        <span class="small" style="color:var(--co-text-secondary, #A1A1AA);">
                            <t t-if="partner.city" t-esc="partner.city"/>
                            <t t-if="partner.city and partner.country_id">, </t>
                            <t t-if="partner.country_id" t-esc="partner.country_id.name"/>
                        </span>
                    </div>
                </t>
                <t t-if="partner.street">
                    <div class="d-flex align-items-center gap-2">
                        <i class="fas fa-home" style="width:18px;color:var(--co-text-muted, #71717A);font-size:0.85rem;"></i>
                        <span class="small" style="color:var(--co-text-secondary, #A1A1AA);" t-esc="partner.street"/>
                    </div>
                </t>
            </div>
        </xpath>
        <!-- Replace the edit link with a styled button -->
        <xpath expr="//a[@href='/my/account']" position="replace">
            <a role="button" href="/my/account" class="btn btn-outline-light btn-sm w-100 mt-3">
                <i class="fas fa-pencil-alt me-1"></i> Edit Information
            </a>
        </xpath>
    </template>

    <!-- ============================================================
         CloudOdoo Layout — wrapper for our custom pages.
         Now just calls website.layout (which already has our
         overridden header/footer). No CDN needed since Odoo 18
         already provides Bootstrap 5.
         ============================================================ -->

    <template id="cloudodoo_layout" name="CloudOdoo Layout">
        <t t-call="website.layout">
            <t t-set="no_livechat" t-value="True"/>
            <!-- Hide Odoo's website header/footer; we render the VELTNEX
                 SPA chrome below so payment / portal pages match the rest
                 of the site (logo, header, footer, colours). -->
            <t t-set="no_header" t-value="True"/>
            <t t-set="no_footer" t-value="True"/>

            <!-- VELTNEX design tokens (mirrors veltnex/src/index.css) +
                 SPA-matching header/footer styling. -->
            <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&amp;display=swap" rel="stylesheet"/>
            <style>
                :root{--vx-bg:248 249 250;--vx-card:255 255 255;--vx-border:218 220 224;--vx-fg:32 33 36;--vx-muted:95 99 104;--vx-primary:26 115 232;}
                html.dark,[data-theme="dark"]{--vx-bg:32 33 36;--vx-card:41 42 45;--vx-border:60 64 67;--vx-fg:232 234 237;--vx-muted:154 160 166;--vx-primary:138 180 248;}
                /* Hide Odoo's website header/footer (the theme ignores
                   no_header/no_footer) — we render the VELTNEX chrome inside
                   main, so these direct-child selectors don't touch ours. */
                #wrapwrap > header, #wrapwrap > footer,
                header#top, #wrapwrap > .o_footer, .o_footer_copyright{display:none !important;}
                body, #wrapwrap, main{background:rgb(var(--vx-bg)) !important;color:rgb(var(--vx-fg));}
                body{font-family:'Roboto',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;}
                .vx-header{position:sticky;top:0;z-index:40;display:flex;align-items:center;justify-content:space-between;height:56px;padding:0 1rem;background:rgb(var(--vx-card));border-bottom:1px solid rgb(var(--vx-border));}
                .vx-logo{display:inline-flex;align-items:center;gap:.6rem;text-decoration:none;color:rgb(var(--vx-fg));font-weight:700;font-size:1rem;}
                .vx-mark{display:inline-flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:.5rem;background:rgb(var(--vx-primary));box-shadow:0 0 20px rgb(var(--vx-primary) / .35);}
                .vx-nex{color:rgb(var(--vx-primary));}
                .vx-link{color:rgb(var(--vx-muted));text-decoration:none;font-size:.875rem;}
                .vx-link:hover{color:rgb(var(--vx-fg));}
                .vx-footer{border-top:1px solid rgb(var(--vx-border));background:rgb(var(--vx-bg));color:rgb(var(--vx-muted));font-size:.75rem;}
                .vx-footer-in{max-width:80rem;margin:0 auto;display:flex;align-items:center;flex-wrap:wrap;gap:.5rem 1.25rem;padding:1.25rem 1rem;}
                .vx-footer a{color:rgb(var(--vx-muted));text-decoration:none;}
                .vx-footer a:hover{color:rgb(var(--vx-fg));}
            </style>
            <script>
                (function(){
                    function apply(t){
                        document.documentElement.setAttribute('data-theme', t);
                        document.documentElement.classList.toggle('dark', t === 'dark');
                    }
                    try {
                        window.addEventListener('storage', function(e){
                            if (e.key === 'veltnex-theme'
                                &amp;&amp; (e.newValue === 'light' || e.newValue === 'dark')) {
                                apply(e.newValue);
                            }
                        });
                    } catch(e) {}
                })();
            </script>

            <header class="vx-header">
                <a href="/" class="vx-logo">
                    <span class="vx-mark">
                        <svg viewBox="0 0 32 32" width="20" height="20" aria-hidden="true">
                            <path d="M8 9 L16 23 L24 9" fill="none" stroke="#FAFAFA" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                    </span>
                    <span>VELT<span class="vx-nex">NEX</span></span>
                </a>
                <a href="/my/instances" class="vx-link">Dashboard</a>
            </header>

            <div id="wrap" class="oe_structure">
                <t t-out="0"/>
            </div>

            <footer class="vx-footer">
                <div class="vx-footer-in">
                    <span class="vx-mark" style="width:24px;height:24px;border-radius:.4rem;">
                        <svg viewBox="0 0 32 32" width="15" height="15" aria-hidden="true">
                            <path d="M8 9 L16 23 L24 9" fill="none" stroke="#FAFAFA" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                    </span>
                    <span>© VELTNEX, Inc.</span>
                    <a href="/hosting">Hosting</a>
                    <a href="/docs">Docs</a>
                    <a href="/help">Help</a>
                    <a href="/my/instances">Dashboard</a>
                </div>
            </footer>
        </t>
    </template>

    <!-- ============================================================
         Home Page (Landing)
         ============================================================ -->


</odoo>
