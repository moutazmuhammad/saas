<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================
         Portal Home: replace with CloudOdoo-styled dashboard
         ============================================================ -->

    <template id="portal_my_home_saas" name="CloudOdoo Portal Home"
              inherit_id="portal.portal_my_home" customize_show="False">

        <!-- Replace the entire portal home content -->
        <xpath expr="//div[hasclass('o_portal_my_home')]" position="replace">
            <div class="o_portal_my_home">
                <div style="padding-top:20px;">

                    <!-- Welcome header -->
                    <div class="d-flex flex-wrap align-items-center justify-content-between mb-4">
                        <div>
                            <h2 class="mb-1">
                                Welcome back, <t t-esc="request.env.user.name"/>
                            </h2>
                            <p class="mb-0" style="color:var(--co-text-muted, #71717A);">
                                Manage your account, instances, and billing from here.
                            </p>
                        </div>
                        <a href="/services" class="btn btn-primary mt-2 mt-md-0">
                            <i class="fas fa-plus me-1"></i>New Instance
                        </a>
                    </div>

                    <!-- Quick action cards -->
                    <div class="row g-3 mb-4">

                        <!-- SaaS Instances card -->
                        <div class="col-md-6 col-lg-4">
                            <a href="/my/instances" class="text-decoration-none">
                                <div class="info-card h-100" style="transition:all 0.2s ease;border-left:3px solid var(--co-primary, #203c86);">
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="d-flex align-items-center justify-content-center rounded"
                                             style="width:48px;height:48px;background:rgba(32,60,134,0.1);color:var(--co-primary, #203c86);font-size:1.25rem;flex-shrink:0;">
                                            <i class="fas fa-server"></i>
                                        </div>
                                        <div>
                                            <h6 class="mb-0 fw-semibold">My Instances</h6>
                                            <p class="mb-0 small" style="color:var(--co-text-muted, #71717A);">
                                                Manage your cloud instances
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </a>
                        </div>

                        <!-- Orders card -->
                        <div class="col-md-6 col-lg-4">
                            <a href="/my/orders" class="text-decoration-none">
                                <div class="info-card h-100" style="transition:all 0.2s ease;border-left:3px solid var(--co-success, #10B981);">
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="d-flex align-items-center justify-content-center rounded"
                                             style="width:48px;height:48px;background:rgba(16,185,129,0.1);color:var(--co-success, #10B981);font-size:1.25rem;flex-shrink:0;">
                                            <i class="fas fa-file-invoice-dollar"></i>
                                        </div>
                                        <div>
                                            <h6 class="mb-0 fw-semibold">Orders</h6>
                                            <p class="mb-0 small" style="color:var(--co-text-muted, #71717A);">
                                                View your purchase history
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </a>
                        </div>

                        <!-- Invoices card -->
                        <div class="col-md-6 col-lg-4">
                            <a href="/my/invoices" class="text-decoration-none">
                                <div class="info-card h-100" style="transition:all 0.2s ease;border-left:3px solid var(--co-warning, #F59E0B);">
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="d-flex align-items-center justify-content-center rounded"
                                             style="width:48px;height:48px;background:rgba(245,158,11,0.1);color:var(--co-warning, #F59E0B);font-size:1.25rem;flex-shrink:0;">
                                            <i class="fas fa-receipt"></i>
                                        </div>
                                        <div>
                                            <h6 class="mb-0 fw-semibold">Invoices</h6>
                                            <p class="mb-0 small" style="color:var(--co-text-muted, #71717A);">
                                                View and pay invoices
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </a>
                        </div>

                        <!-- Security card -->
                        <div class="col-md-6 col-lg-4">
                            <a href="/my/security" class="text-decoration-none">
                                <div class="info-card h-100" style="transition:all 0.2s ease;border-left:3px solid var(--co-info, #3B82F6);">
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="d-flex align-items-center justify-content-center rounded"
                                             style="width:48px;height:48px;background:rgba(59,130,246,0.1);color:var(--co-info, #3B82F6);font-size:1.25rem;flex-shrink:0;">
                                            <i class="fas fa-shield-alt"></i>
                                        </div>
                                        <div>
                                            <h6 class="mb-0 fw-semibold">Security</h6>
                                            <p class="mb-0 small" style="color:var(--co-text-muted, #71717A);">
                                                Password &amp; connection settings
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </a>
                        </div>

                        <!-- Browse Services card -->
                        <div class="col-md-6 col-lg-4">
                            <a href="/services" class="text-decoration-none">
                                <div class="info-card h-100" style="transition:all 0.2s ease;border-left:3px solid #8B5CF6;">
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="d-flex align-items-center justify-content-center rounded"
                                             style="width:48px;height:48px;background:rgba(139,92,246,0.1);color:#8B5CF6;font-size:1.25rem;flex-shrink:0;">
                                            <i class="fas fa-th-large"></i>
                                        </div>
                                        <div>
                                            <h6 class="mb-0 fw-semibold">Browse Services</h6>
                                            <p class="mb-0 small" style="color:var(--co-text-muted, #71717A);">
                                                Explore available cloud solutions
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </a>
                        </div>

                    </div>

                    <!-- Keep original portal docs for any other modules that inject entries -->
                    <div class="o_portal_docs row g-2" style="display:none !important;">
                        <div t-if="portal_alert_category_enable" class="o_portal_category row g-2 mt-3" id="portal_alert_category"/>
                        <div t-if="portal_client_category_enable" class="o_portal_category row g-2 mt-3" id="portal_client_category"/>
                        <div t-if="portal_service_category_enable" class="o_portal_category row g-2 mt-3" id="portal_service_category"/>
                        <div t-if="portal_vendor_category_enable" class="o_portal_category row g-2 mt-3" id="portal_vendor_category"/>
                        <div class="o_portal_category row g-2 mt-3" id="portal_common_category">
                            <t t-call="portal.portal_docs_entry" t-if="False"/>
                        </div>
                        <div class="o_portal_doc_spinner spinner-border text-o-color-2 align-self-center mt-5"/>
                    </div>

                </div>
            </div>
        </xpath>
    </template>

    <!-- ============================================================
         My Instances List  —  /my/instances
         ============================================================ -->

    <template id="portal_my_instances" name="My Instances">
        <t t-call="saas_website.cloudodoo_layout">
            <section class="py-6" style="padding-top:120px;">
                <div class="container">
                    <div class="row">
                        <!-- Sidebar -->
                        <div class="col-lg-3 mb-4">
                            <div class="portal-sidebar">
                                <nav class="nav flex-column">
                                    <a class="nav-link active" href="/my/instances">
                                        <i class="fas fa-server"></i> My Instances
                                    </a>
                                    <a class="nav-link" href="/my/home">
                                        <i class="fas fa-user"></i> Profile
                                    </a>
                                </nav>
                            </div>
                        </div>

                        <!-- Main Content -->
                        <div class="col-lg-9">
                            <div class="d-flex flex-wrap justify-content-between align-items-center mb-4">
                                <div>
                                    <h1 class="h3 mb-1">My Instances</h1>
                                    <p class="text-muted mb-0">Manage your deployed cloud instances</p>
                                </div>
                                <div class="d-flex align-items-center gap-3 mt-3 mt-md-0">
                                    <select class="form-select form-select-sm" id="sort-select" style="width:auto;">
                                        <t t-foreach="searchbar_sortings.items()" t-as="option">
                                            <option t-att-value="option[0]"
                                                    t-att-selected="option[0] == sortby"
                                                    t-esc="option[1]['label']"/>
                                        </t>
                                    </select>
                                    <a href="/services" class="btn btn-primary btn-sm">
                                        <i class="fas fa-plus me-1"></i>New Instance
                                    </a>
                                </div>
                            </div>

                            <!-- Folder Tabs -->
                            <div class="d-flex align-items-center gap-2 mb-3 flex-wrap" id="folder-tabs">
                                <a t-attf-href="/my/instances?sortby=#{sortby}"
                                   t-attf-class="btn btn-sm #{active_folder == 'all' and 'btn-primary' or 'btn-outline-secondary'}">
                                    All <span class="badge bg-light text-dark ms-1" t-esc="all_count"/>
                                </a>
                                <a t-attf-href="/my/instances?sortby=#{sortby}&amp;folder=unfiled"
                                   t-attf-class="btn btn-sm #{active_folder == 'unfiled' and 'btn-primary' or 'btn-outline-secondary'}">
                                    <i class="fas fa-inbox me-1"></i>Unfiled
                                    <span class="badge bg-light text-dark ms-1" t-esc="unfiled_count"/>
                                </a>
                                <t t-foreach="folders" t-as="f">
                                    <div class="btn-group btn-group-sm folder-tab-group">
                                        <a t-attf-href="/my/instances?sortby=#{sortby}&amp;folder=#{f.id}"
                                           t-attf-class="btn #{str(active_folder_id) == str(f.id) and 'btn-primary' or 'btn-outline-secondary'}">
                                            <i class="fas fa-folder me-1"></i>
                                            <span class="folder-name" t-esc="f.name"/>
                                            <span class="badge bg-light text-dark ms-1" t-esc="f.instance_count"/>
                                        </a>
                                        <button type="button"
                                                t-attf-class="btn dropdown-toggle dropdown-toggle-split #{str(active_folder_id) == str(f.id) and 'btn-primary' or 'btn-outline-secondary'}"
                                                data-bs-toggle="dropdown" aria-expanded="false">
                                            <span class="visually-hidden">Toggle Dropdown</span>
                                        </button>
                                        <ul class="dropdown-menu dropdown-menu-end">
                                            <li>
                                                <a class="dropdown-item folder-rename-btn" href="#"
                                                   t-att-data-folder-id="f.id"
                                                   t-att-data-folder-name="f.name">
                                                    <i class="fas fa-pen me-2"></i>Rename
                                                </a>
                                            </li>
                                            <li><hr class="dropdown-divider"/></li>
                                            <li>
                                                <a class="dropdown-item text-danger folder-delete-btn" href="#"
                                                   t-att-data-folder-id="f.id"
                                                   t-att-data-folder-name="f.name">
                                                    <i class="fas fa-trash me-2"></i>Delete
                                                </a>
                                            </li>
                                        </ul>
                                    </div>
                                </t>
                                <button class="btn btn-sm btn-outline-secondary" id="btn-create-folder"
                                        title="Create folder">
                                    <i class="fas fa-folder-plus"></i>
                                </button>
                            </div>

                            <div class="info-card">
                                <t t-if="instances">
                                    <div class="table-responsive">
                                        <table class="table table-dark-custom">
                                            <thead>
                                                <tr>
                                                    <th style="width:30px;">
                                                        <input type="checkbox" class="form-check-input" id="select-all-instances"/>
                                                    </th>
                                                    <th>Instance</th>
                                                    <th>Service</th>
                                                    <th>Plan</th>
                                                    <th>Status</th>
                                                    <th>URL</th>
                                                    <th>Created</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <t t-foreach="instances" t-as="inst">
                                                    <tr>
                                                        <td>
                                                            <input type="checkbox" class="form-check-input instance-checkbox"
                                                                   t-att-value="inst.id"/>
                                                        </td>
                                                        <td>
                                                            <a t-attf-href="/my/instances/#{inst.id}"
                                                               class="fw-semibold text-primary"
                                                               t-esc="inst.subdomain or inst.name"/>
                                                            <t t-if="inst.folder_id and active_folder == 'all'">
                                                                <span class="badge bg-secondary ms-1 small"
                                                                      style="font-size:0.7em;">
                                                                    <i class="fas fa-folder me-1"></i>
                                                                    <t t-esc="inst.folder_id.name"/>
                                                                </span>
                                                            </t>
                                                        </td>
                                                        <td t-esc="inst.saas_product_id.name"/>
                                                        <td>
                                                            <t t-esc="inst.plan_id.name"/>
                                                            <t t-if="inst.is_trial">
                                                                <span class="badge bg-info ms-1">Trial</span>
                                                            </t>
                                                        </td>
                                                        <td>
                                                            <span t-attf-class="badge-status badge-#{inst.state}">
                                                                <t t-if="inst.state == 'running'"><i class="fas fa-circle"></i> </t>
                                                                <t t-if="inst.state in ('provisioning', 'pending_provision')">
                                                                    <span class="spinner-sm"></span>
                                                                </t>
                                                                <t t-esc="dict(inst._fields['state'].selection).get(inst.state, inst.state)"/>
                                                            </span>
                                                        </td>
                                                        <td>
                                                            <t t-if="inst.state == 'running' and inst.url">
                                                                <a t-att-href="inst.url" target="_blank" class="text-muted small">
                                                                    <t t-esc="inst.subdomain"/>.<t t-esc="inst.domain_id.name"/>
                                                                    <i class="fas fa-external-link-alt ms-1"></i>
                                                                </a>
                                                            </t>
                                                            <t t-else="">
                                                                <span class="text-muted">—</span>
                                                            </t>
                                                        </td>
                                                        <td class="text-muted">
                                                            <t t-esc="inst.create_date" t-options="{'widget': 'date'}"/>
                                                        </td>
                                                    </tr>
                                                </t>
                                            </tbody>
                                        </table>
                                    </div>

                                    <!-- Bulk action bar (shown when instances selected) -->
                                    <div class="d-none align-items-center gap-2 p-2 border-top" id="bulk-actions">
                                        <span class="text-muted small me-2">
                                            <span id="selected-count">0</span> selected
                                        </span>
                                        <div class="dropdown">
                                            <button class="btn btn-sm btn-outline-secondary dropdown-toggle"
                                                    type="button" data-bs-toggle="dropdown">
                                                <i class="fas fa-folder me-1"></i>Move to folder
                                            </button>
                                            <ul class="dropdown-menu" id="move-folder-menu">
                                                <li>
                                                    <a class="dropdown-item move-to-folder" href="#" data-folder-id="0">
                                                        <i class="fas fa-inbox me-2"></i>Unfiled
                                                    </a>
                                                </li>
                                                <t t-foreach="folders" t-as="f">
                                                    <li>
                                                        <a class="dropdown-item move-to-folder" href="#"
                                                           t-att-data-folder-id="f.id">
                                                            <i class="fas fa-folder me-2"></i>
                                                            <t t-esc="f.name"/>
                                                        </a>
                                                    </li>
                                                </t>
                                            </ul>
                                        </div>
                                    </div>
                                </t>

                                <!-- Empty state -->
                                <t t-if="not instances">
                                    <div class="empty-state">
                                        <div class="empty-state-icon">
                                            <t t-if="active_folder != 'all'">
                                                <i class="fas fa-folder-open"></i>
                                            </t>
                                            <t t-else="">
                                                <i class="fas fa-server"></i>
                                            </t>
                                        </div>
                                        <t t-if="active_folder != 'all'">
                                            <h5>No instances in this folder</h5>
                                            <p>Move instances here to organize your work.</p>
                                        </t>
                                        <t t-else="">
                                            <h5>No instances yet</h5>
                                            <p>Create your first cloud instance to get started.</p>
                                        </t>
                                        <a href="/services" class="btn btn-primary">
                                            <i class="fas fa-plus me-2"></i>Browse Services
                                        </a>
                                    </div>
                                </t>
                            </div>

                            <!-- Pager -->
                            <t t-if="pager" t-call="portal.pager"/>
                        </div>
                    </div>
                </div>
            </section>
        </t>
    </template>

    <!-- ============================================================
         Instance Detail  —  /my/instances/<id>
         ============================================================ -->

    <template id="portal_instance_detail" name="Instance Detail">
        <t t-call="saas_website.cloudodoo_layout">
            <section class="py-6" style="padding-top:120px;">
                <div class="container">
                    <!-- Breadcrumb -->
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="/my/instances">My Instances</a></li>
                            <li class="breadcrumb-item active" t-esc="instance.subdomain or instance.name"/>
                        </ol>
                    </nav>

                    <div class="row g-4">
                        <!-- Left Column -->
                        <div class="col-lg-8">

                            <!-- Instance Info Card -->
                            <div class="info-card">
                                <div class="d-flex justify-content-between align-items-start mb-3">
                                    <div>
                                        <h4 class="mb-1" t-esc="instance.subdomain or instance.name"/>
                                        <span t-attf-class="badge-status badge-#{instance.state}">
                                            <t t-if="instance.state == 'running'"><i class="fas fa-circle"></i> </t>
                                            <t t-if="instance.state in ('provisioning', 'pending_provision')">
                                                <span class="spinner-sm"></span>
                                            </t>
                                            <t t-esc="dict(instance._fields['state'].selection).get(instance.state, instance.state)"/>
                                        </span>
                                    </div>
                                </div>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <div class="text-muted small">URL</div>
                                        <t t-if="instance.state == 'running' and instance.url">
                                            <a t-att-href="instance.url" target="_blank">
                                                <t t-esc="instance.url"/> <i class="fas fa-external-link-alt ms-1"></i>
                                            </a>
                                        </t>
                                        <t t-else=""><span class="text-muted">Not available</span></t>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="text-muted small">Service</div>
                                        <div t-esc="instance.saas_product_id.name"/>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="text-muted small">Plan</div>
                                        <div>
                                            <t t-esc="instance.plan_id.name"/>
                                            <t t-if="instance.is_trial">
                                                <span class="badge bg-info">Trial</span>
                                            </t>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="text-muted small">Domain</div>
                                        <div><t t-esc="instance.subdomain"/>.<t t-esc="instance.domain_id.name"/></div>
                                    </div>
                                </div>
                            </div>

                            <!-- Provisioning Status Polling -->
                            <t t-if="instance.state in ('provisioning', 'pending_provision')">
                                <div class="alert-banner alert-info" id="provisioning-poll"
                                     t-att-data-instance-id="instance.id">
                                    <i class="fas fa-spinner fa-spin"></i>
                                    <div>
                                        <strong>Setting Up Your Instance</strong>
                                        <p class="mb-0">This usually takes a few minutes. The page will refresh automatically.</p>
                                    </div>
                                </div>
                            </t>

                            <!-- Trial Alert -->
                            <t t-if="instance.is_trial and instance.state == 'running'">
                                <div class="alert-banner alert-info">
                                    <i class="fas fa-info-circle"></i>
                                    <div class="flex-grow-1">
                                        <strong>Free Trial Active</strong>
                                        <p class="mb-2">
                                            <t t-if="instance.partner_id.sudo().saas_trial_end_date">
                                                Your trial ends on
                                                <strong t-esc="instance.partner_id.sudo().saas_trial_end_date"
                                                        t-options="{'widget': 'date'}"/>.
                                                Upgrade to a paid plan before then to keep your instance running.
                                            </t>
                                            <t t-else="">
                                                Your trial is active. Upgrade to a paid plan to keep your instance running.
                                            </t>
                                        </p>
                                        <a t-attf-href="/my/instances/#{instance.id}/upgrade"
                                           class="btn btn-sm btn-primary">Upgrade Now</a>
                                    </div>
                                </div>
                            </t>

                            <!-- Pending Payment Alert -->
                            <t t-if="instance.state == 'pending_payment'">
                                <div class="alert-banner alert-warning">
                                    <i class="fas fa-exclamation-triangle"></i>
                                    <div class="flex-grow-1">
                                        <strong>Payment Required</strong>
                                        <p class="mb-2">Complete your payment to activate this instance.</p>
                                        <a t-attf-href="/my/instances/#{instance.id}/checkout"
                                           class="btn btn-sm btn-warning">Complete Payment</a>
                                    </div>
                                </div>
                            </t>

                            <!-- Pending Upgrade (awaiting payment) -->
                            <t t-if="instance.pending_plan_id">
                                <div class="alert-banner alert-info">
                                    <i class="fas fa-arrow-up"></i>
                                    <div class="flex-grow-1">
                                        <strong>Upgrade Pending Payment</strong>
                                        <p class="mb-2">
                                            You selected the <strong t-esc="instance.pending_plan_id.name"/> plan
                                            (<t t-esc="instance.pending_billing_period or 'monthly'"/>).
                                            Complete your payment to apply the upgrade, or choose a different plan.
                                        </p>
                                        <div class="d-flex gap-2 flex-wrap">
                                            <a t-attf-href="/my/instances/#{instance.id}/checkout"
                                               class="btn btn-sm btn-primary">
                                                <i class="fas fa-credit-card me-1"></i>Pay Now
                                            </a>
                                            <t t-if="not instance.is_trial">
                                                <a t-attf-href="/my/instances/#{instance.id}/change-plan"
                                                   class="btn btn-sm btn-outline-light">
                                                    <i class="fas fa-exchange-alt me-1"></i>Choose Different Plan
                                                </a>
                                            </t>
                                            <t t-if="instance.is_trial">
                                                <a t-attf-href="/my/instances/#{instance.id}/upgrade"
                                                   class="btn btn-sm btn-outline-light">
                                                    <i class="fas fa-exchange-alt me-1"></i>Choose Different Plan
                                                </a>
                                            </t>
                                            <form t-attf-action="/my/instances/#{instance.id}/cancel-upgrade"
                                                  method="post" style="display:inline;">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                <button type="submit" class="btn btn-sm btn-outline-danger"
                                                        data-confirm="Cancel this upgrade? Your current plan will remain unchanged."
                                                        data-confirm-variant="danger"
                                                        data-confirm-yes="Yes, cancel upgrade"
                                                        data-confirm-no="No, keep it">
                                                    <i class="fas fa-times me-1"></i>Cancel Upgrade
                                                </button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </t>

                            <!-- Scheduled Change (downgrade or billing period switch) -->
                            <t t-if="instance.scheduled_plan_id">
                                <t t-set="is_period_switch" t-value="instance.scheduled_plan_id.id == instance.plan_id.id"/>
                                <div t-attf-class="alert-banner #{'alert-info' if is_period_switch else 'alert-warning'}">
                                    <i t-attf-class="fas #{'fa-sync-alt' if is_period_switch else 'fa-arrow-down'}"></i>
                                    <div class="flex-grow-1">
                                        <t t-if="is_period_switch">
                                            <strong>Billing Period Change Scheduled</strong>
                                            <p class="mb-2">
                                                Your billing will switch from
                                                <strong t-esc="instance.billing_period or 'monthly'"/> to
                                                <strong t-esc="instance.scheduled_billing_period or 'monthly'"/>
                                                on your next billing cycle
                                                <t t-if="instance.next_invoice_date">
                                                    (<t t-esc="instance.next_invoice_date" t-options="{'widget': 'date'}"/>)
                                                </t>.
                                                Your plan (<strong t-esc="instance.plan_id.name"/>) stays the same.
                                            </p>
                                        </t>
                                        <t t-else="">
                                            <strong>Downgrade Scheduled</strong>
                                            <p class="mb-2">
                                                Your plan will change from
                                                <strong t-esc="instance.plan_id.name"/>
                                                (<t t-esc="instance.billing_period or 'monthly'"/>) to
                                                <strong t-esc="instance.scheduled_plan_id.name"/>
                                                (<t t-esc="instance.scheduled_billing_period or 'monthly'"/>)
                                                at the end of your current billing cycle
                                                <t t-if="instance.next_invoice_date">
                                                    (<t t-esc="instance.next_invoice_date" t-options="{'widget': 'date'}"/>)
                                                </t>.
                                            </p>
                                            <div class="d-flex gap-2">
                                                <div class="info-card p-3 mb-2" style="background:rgba(255,255,255,0.05);flex:1;">
                                                    <div class="text-muted small">New Plan Resources</div>
                                                    <div class="mt-1">
                                                        <span class="me-3"><i class="fas fa-microchip me-1"></i><t t-esc="instance.scheduled_plan_id.cpu_limit"/> vCPU</span>
                                                        <span class="me-3"><i class="fas fa-memory me-1"></i><t t-esc="instance.scheduled_plan_id.ram_limit"/> RAM</span>
                                                        <span><i class="fas fa-hdd me-1"></i><t t-esc="int(instance.scheduled_plan_id.storage_limit)"/>GB</span>
                                                    </div>
                                                </div>
                                            </div>
                                        </t>
                                        <form t-attf-action="/my/instances/#{instance.id}/cancel-downgrade"
                                              method="post" class="mt-2" style="display:inline;">
                                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                            <button type="submit" class="btn btn-sm btn-outline-light"
                                                    t-att-data-confirm="'Cancel this billing period change and keep your current settings?' if is_period_switch else 'Cancel the scheduled downgrade and keep your current plan?'"
                                                    data-confirm-variant="warning"
                                                    t-att-data-confirm-yes="'Yes, cancel change' if is_period_switch else 'Yes, cancel downgrade'"
                                                    data-confirm-no="No, keep it">
                                                <i class="fas fa-times me-1"></i>
                                                <t t-if="is_period_switch">Cancel Period Change</t>
                                                <t t-else="">Cancel Downgrade</t>
                                            </button>
                                        </form>
                                    </div>
                                </div>
                            </t>

                            <!-- Resource Usage -->
                            <t t-if="instance.state == 'running'">
                                <div class="info-card">
                                    <div class="d-flex justify-content-between align-items-center mb-3">
                                        <h5 class="mb-0">Resource Usage</h5>
                                        <button class="btn btn-sm btn-outline-secondary" id="refresh-usage"
                                                t-att-data-instance-id="instance.id">
                                            <i class="fas fa-sync-alt"></i>
                                        </button>
                                    </div>

                                    <t t-set="cpu_pct" t-value="instance.cpu_usage_pct or 0"/>
                                    <t t-set="ram_pct" t-value="instance.ram_usage_pct or 0"/>
                                    <t t-set="storage_pct" t-value="instance.storage_usage_pct or 0"/>

                                    <div class="resource-item">
                                        <div class="resource-header">
                                            <span class="resource-label"><i class="fas fa-microchip me-2"></i>CPU</span>
                                            <span class="resource-value" id="cpu-usage"><t t-esc="instance.cpu_usage or '0%'"/></span>
                                        </div>
                                        <div class="progress">
                                            <div t-attf-class="progress-bar #{'bg-success' if cpu_pct &lt; 60 else 'bg-warning' if cpu_pct &lt; 80 else 'bg-danger'}"
                                                 t-attf-style="width:#{int(cpu_pct)}%" id="cpu-bar"></div>
                                        </div>
                                    </div>

                                    <div class="resource-item">
                                        <div class="resource-header">
                                            <span class="resource-label"><i class="fas fa-memory me-2"></i>RAM</span>
                                            <span class="resource-value" id="ram-usage">
                                                <t t-esc="instance.ram_usage or '0 / 0'"/>
                                            </span>
                                        </div>
                                        <div class="progress">
                                            <div t-attf-class="progress-bar #{'bg-success' if ram_pct &lt; 60 else 'bg-warning' if ram_pct &lt; 80 else 'bg-danger'}"
                                                 t-attf-style="width:#{int(ram_pct)}%" id="ram-bar"></div>
                                        </div>
                                    </div>

                                    <div class="resource-item mb-0">
                                        <div class="resource-header">
                                            <span class="resource-label"><i class="fas fa-hdd me-2"></i>Storage</span>
                                            <span class="resource-value" id="storage-usage">
                                                <t t-esc="instance.disk_usage or '0'"/>
                                            </span>
                                        </div>
                                        <div class="progress">
                                            <div t-attf-class="progress-bar #{'bg-success' if storage_pct &lt; 60 else 'bg-warning' if storage_pct &lt; 80 else 'bg-danger'}"
                                                 t-attf-style="width:#{int(storage_pct)}%" id="storage-bar"></div>
                                        </div>
                                    </div>
                                </div>
                            </t>

                            <!-- Recent upgrade credit notice -->
                            <t t-set="recent_upgrade_inv" t-value="False"/>
                            <t t-foreach="invoices" t-as="rinv">
                                <t t-if="not recent_upgrade_inv and rinv.payment_state in ('paid', 'in_payment') and 'credit' in (rinv.invoice_line_ids[:1].name or '').lower()">
                                    <t t-set="recent_upgrade_inv" t-value="rinv"/>
                                </t>
                            </t>
                            <t t-if="recent_upgrade_inv">
                                <div class="alert-banner alert-success mb-0" style="border-radius:var(--radius-lg, 14px) var(--radius-lg, 14px) 0 0;margin-bottom:-1px !important;">
                                    <i class="fas fa-receipt"></i>
                                    <div>
                                        <strong>Upgrade Credit Applied</strong>
                                        <p class="mb-0 small" style="color:var(--co-text-secondary, #A1A1AA);">
                                            Your remaining balance from the previous plan was deducted from the upgrade cost.
                                            See invoice <strong t-esc="recent_upgrade_inv.name"/> for the full breakdown.
                                        </p>
                                    </div>
                                </div>
                            </t>

                            <!-- Invoices -->
                            <div class="info-card" t-att-style="'border-top-left-radius:0;border-top-right-radius:0;' if recent_upgrade_inv else ''">
                                <h5>Billing History</h5>
                                <t t-if="invoices">
                                    <div class="table-responsive">
                                        <table class="table table-dark-custom mb-0">
                                            <thead>
                                                <tr>
                                                    <th>Invoice</th>
                                                    <th>Date</th>
                                                    <th>Description</th>
                                                    <th>Amount</th>
                                                    <th>Status</th>
                                                    <th></th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <t t-foreach="invoices" t-as="inv">
                                                    <t t-set="inv_unpaid" t-value="inv.state == 'posted' and inv.payment_state not in ('paid', 'in_payment') and inv.amount_residual > 0"/>
                                                    <t t-set="inv_line_name" t-value="inv.invoice_line_ids[:1].name if inv.invoice_line_ids else ''"/>
                                                    <t t-set="has_credit" t-value="'credit' in (inv_line_name or '').lower()"/>
                                                    <tr>
                                                        <td>
                                                            <a t-att-href="inv.get_portal_url()" t-esc="inv.name"/>
                                                        </td>
                                                        <td t-esc="inv.invoice_date" t-options="{'widget': 'date'}"/>
                                                        <td>
                                                            <!-- Show invoice line description with credit highlight -->
                                                            <t t-if="inv_line_name">
                                                                <div class="small" style="max-width:300px;">
                                                                    <t t-esc="inv_line_name.split('(credit')[0].strip() if has_credit else inv_line_name"/>
                                                                </div>
                                                                <t t-if="has_credit">
                                                                    <div class="small mt-1" style="color:#10B981;">
                                                                        <i class="fas fa-receipt me-1"></i>
                                                                        Includes credit for unused days on previous plan
                                                                    </div>
                                                                </t>
                                                            </t>
                                                        </td>
                                                        <td>
                                                            <t t-esc="inv.amount_total" t-options="{'widget': 'monetary', 'display_currency': inv.currency_id}"/>
                                                        </td>
                                                        <td>
                                                            <t t-if="inv.state == 'cancel'">
                                                                <span class="badge-status badge-cancelled">Cancelled</span>
                                                            </t>
                                                            <t t-elif="inv.payment_state in ('paid', 'in_payment')">
                                                                <span class="badge-status badge-running">Paid</span>
                                                            </t>
                                                            <t t-elif="inv.payment_state == 'reversed'">
                                                                <span class="badge-status badge-suspended">Reversed</span>
                                                            </t>
                                                            <t t-else="">
                                                                <span class="badge-status badge-failed">Not Paid</span>
                                                            </t>
                                                        </td>
                                                        <td>
                                                            <!-- Cancel button only for unpaid upgrade invoices -->
                                                            <t t-if="inv_unpaid and instance.pending_plan_id">
                                                                <form t-attf-action="/my/instances/#{instance.id}/cancel-invoice/#{inv.id}"
                                                                      method="post" style="display:inline;">
                                                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                                    <button type="submit" class="btn btn-sm btn-outline-danger"
                                                                            data-confirm="Cancel this invoice and the pending upgrade?"
                                                                            data-confirm-variant="danger"
                                                                            data-confirm-yes="Yes, cancel"
                                                                            data-confirm-no="No, keep it">
                                                                        <i class="fas fa-times"></i>
                                                                    </button>
                                                                </form>
                                                            </t>
                                                        </td>
                                                    </tr>
                                                </t>
                                            </tbody>
                                        </table>
                                    </div>
                                </t>
                                <t t-if="not invoices">
                                    <p class="text-muted mb-0">No invoices yet</p>
                                </t>
                            </div>

                            <!-- Backups -->
                            <div class="info-card">
                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <h5 class="mb-0">Backups</h5>
                                    <div class="d-flex align-items-center gap-3">
                                        <span class="text-muted small">
                                            <t t-esc="len(backups)"/> / <t t-esc="instance.plan_id.max_backups"/> used
                                        </span>
                                        <t t-if="not instance.is_trial and instance.state == 'running'">
                                            <form t-attf-action="/my/instances/#{instance.id}/create-backup"
                                                  method="post" style="display:inline;">
                                                <input type="hidden" name="csrf_token"
                                                       t-att-value="request.csrf_token()"/>
                                                <button type="submit" class="btn btn-sm btn-primary"
                                                        data-confirm="Create a new backup of your instance?"
                                                        data-confirm-variant="info"
                                                        data-confirm-yes="Yes, create backup"
                                                        data-confirm-no="Cancel">
                                                    <i class="fas fa-plus me-1"></i>Create Backup
                                                </button>
                                            </form>
                                        </t>
                                    </div>
                                </div>

                                <t t-if="instance.is_trial">
                                    <p class="text-muted small mb-3">
                                        <i class="fas fa-info-circle me-1"></i>
                                        Backups are not available on the trial plan
                                    </p>
                                </t>

                                <t t-if="backups">
                                    <div class="table-responsive">
                                        <table class="table table-dark-custom mb-0">
                                            <thead>
                                                <tr>
                                                    <th>Backup</th>
                                                    <th>Date</th>
                                                    <th>Status</th>
                                                    <th>Actions</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <t t-foreach="backups" t-as="backup">
                                                    <tr>
                                                        <td t-esc="backup.name"/>
                                                        <td t-esc="backup.create_date" t-options="{'widget': 'date'}"/>
                                                        <td>
                                                            <span t-attf-class="badge-status #{'badge-running' if backup.state == 'done' else 'badge-provisioning'}">
                                                                <t t-if="backup.state == 'running'"><span class="spinner-sm"></span> </t>
                                                                <t t-esc="backup.state"/>
                                                            </span>
                                                        </td>
                                                        <td>
                                                            <t t-if="backup.state == 'done' and instance.state == 'running'">
                                                                <form t-attf-action="/my/instances/#{instance.id}/backup/#{backup.id}/restore"
                                                                      method="post" style="display:inline;">
                                                                    <input type="hidden" name="csrf_token"
                                                                           t-att-value="request.csrf_token()"/>
                                                                    <button type="submit"
                                                                            class="btn btn-sm btn-outline-secondary"
                                                                            data-confirm="Restore your instance from this backup? Current data will be replaced."
                                                                            data-confirm-variant="warning"
                                                                            data-confirm-yes="Yes, restore"
                                                                            data-confirm-no="Cancel">
                                                                        <i class="fas fa-undo"></i> Restore
                                                                    </button>
                                                                </form>
                                                            </t>
                                                        </td>
                                                    </tr>
                                                </t>
                                            </tbody>
                                        </table>
                                    </div>
                                </t>
                                <t t-if="not backups and not instance.is_trial">
                                    <p class="text-muted mb-0">No backups yet</p>
                                </t>
                            </div>
                        </div>

                        <!-- Right Column - Sidebar -->
                        <div class="col-lg-4">
                            <!-- Actions -->
                            <div class="actions-card">
                                <h6>Actions</h6>

                                <t t-if="instance.state == 'running'">
                                    <a t-att-href="instance.url" target="_blank"
                                       class="action-btn action-primary">
                                        <i class="fas fa-external-link-alt"></i>Open Instance
                                    </a>
                                    <button class="action-btn" data-saas-action="restart"
                                            t-att-data-instance-id="instance.id">
                                        <i class="fas fa-redo"></i>Restart
                                    </button>
                                    <button class="action-btn" data-saas-action="stop"
                                            t-att-data-instance-id="instance.id">
                                        <i class="fas fa-stop"></i>Stop
                                    </button>
                                </t>

                                <t t-if="instance.state == 'stopped'">
                                    <button class="action-btn action-primary" data-saas-action="start"
                                            t-att-data-instance-id="instance.id">
                                        <i class="fas fa-play"></i>Start
                                    </button>
                                </t>

                                <t t-if="instance.is_trial">
                                    <a t-attf-href="/my/instances/#{instance.id}/upgrade"
                                       class="action-btn">
                                        <i class="fas fa-arrow-up"></i>Upgrade Plan
                                    </a>
                                </t>
                                <t t-if="not instance.is_trial and instance.state in ('running', 'stopped')">
                                    <a t-attf-href="/my/instances/#{instance.id}/change-plan"
                                       class="action-btn">
                                        <i class="fas fa-exchange-alt"></i>Change Plan
                                    </a>
                                </t>
                            </div>

                            <!-- Current Plan -->
                            <div class="info-card mt-4" t-if="instance.plan_id">
                                <h6 class="text-muted text-uppercase small mb-3">Current Plan</h6>
                                <h5 class="mb-3" t-esc="instance.plan_id.name"/>
                                <ul class="plan-specs mb-0">
                                    <li><i class="fas fa-microchip"></i><t t-esc="instance.plan_id.cpu_limit"/> vCPU</li>
                                    <li><i class="fas fa-memory"></i><t t-esc="instance.plan_id.ram_limit"/> RAM</li>
                                    <li><i class="fas fa-hdd"></i><t t-esc="int(instance.plan_id.storage_limit)"/>GB Storage</li>
                                    <li><i class="fas fa-shield-alt"></i><t t-esc="instance.plan_id.max_backups"/> Backups</li>
                                </ul>

                                <t t-if="not instance.is_trial and instance.next_invoice_date">
                                    <div class="mt-3 pt-3 border-top" style="border-color:var(--border-color)!important;">
                                        <div class="text-muted small">Next billing date</div>
                                        <div t-esc="instance.next_invoice_date" t-options="{'widget': 'date'}"/>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </t>
    </template>

    <!-- ============================================================
         Upgrade Plan (Trial to Paid)  —  /my/instances/<id>/upgrade
         ============================================================ -->

    <template id="portal_instance_upgrade" name="Upgrade Plan">
        <t t-call="saas_website.cloudodoo_layout">
            <section class="py-6" style="padding-top:120px;">
                <div class="container">
                    <div class="page-header">
                        <nav aria-label="breadcrumb">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="/my/instances">My Instances</a></li>
                                <li class="breadcrumb-item">
                                    <a t-attf-href="/my/instances/#{instance.id}"
                                       t-esc="instance.subdomain or instance.name"/>
                                </li>
                                <li class="breadcrumb-item active">Upgrade</li>
                            </ol>
                        </nav>
                        <h1>Upgrade Your Plan</h1>
                        <p>
                            Your trial instance <strong t-esc="instance.subdomain"/> is currently on the
                            <strong t-esc="instance.plan_id.name"/> plan (Free Trial).
                            Choose a paid plan below to keep your data and instance running.
                        </p>
                    </div>

                    <!-- Trial info banner -->
                    <div class="alert-banner alert-info mb-4">
                        <i class="fas fa-info-circle"></i>
                        <div>
                            <strong>What happens when you upgrade?</strong>
                            <ul class="list-unstyled mb-0 mt-2" style="color:var(--co-text-secondary, #A1A1AA);">
                                <li class="mb-1"><i class="fas fa-check text-success me-2"></i>Your data and configuration are preserved</li>
                                <li class="mb-1"><i class="fas fa-check text-success me-2"></i>Resources are upgraded instantly (zero downtime)</li>
                                <li class="mb-1"><i class="fas fa-check text-success me-2"></i>Backups become available</li>
                                <li class="mb-1"><i class="fas fa-check text-success me-2"></i>Your subdomain stays the same</li>
                            </ul>
                        </div>
                    </div>

                    <t t-if="error">
                        <div class="alert-banner alert-danger mb-4">
                            <i class="fas fa-exclamation-circle"></i>
                            <div t-esc="error"/>
                        </div>
                    </t>

                    <div class="row g-4">
                        <t t-foreach="plans" t-as="plan">
                            <div class="col-md-6 col-lg-4">
                                <div class="plan-card">
                                    <h4 class="plan-name" t-esc="plan.name"/>

                                    <!-- Monthly price -->
                                    <div class="plan-price">
                                        <span class="amount" t-esc="'${:,.2f}'.format(plan.price)"/>
                                        <span class="period">/mo</span>
                                    </div>

                                    <!-- Yearly savings callout -->
                                    <t t-if="plan.yearly_price and plan.yearly_price > 0">
                                        <div class="mb-3 p-2" style="background:rgba(16,185,129,0.08);border:1px solid rgba(16,185,129,0.2);border-radius:8px;font-size:0.85rem;">
                                            <i class="fas fa-tag me-1" style="color:#10B981;"></i>
                                            <strong>Yearly:</strong>
                                            <t t-esc="'${:,.2f}'.format(plan.yearly_price)"/>/yr
                                            (<t t-esc="'${:,.2f}'.format(plan.yearly_price / 12)"/>/mo)
                                            <t t-if="plan.yearly_discount_pct > 0">
                                                <span style="background:rgba(16,185,129,0.2);color:#10B981;font-size:0.75rem;font-weight:600;padding:0.1rem 0.5rem;border-radius:20px;margin-left:0.25rem;">
                                                    Save <t t-esc="'%.0f' % plan.yearly_discount_pct"/>%
                                                </span>
                                            </t>
                                        </div>
                                    </t>

                                    <!-- Resource comparison vs trial -->
                                    <ul class="plan-specs">
                                        <li>
                                            <i class="fas fa-microchip"></i>
                                            <t t-esc="plan.cpu_limit"/> vCPU
                                            <t t-if="plan.cpu_limit > (instance.plan_id.cpu_limit or 0)">
                                                <span style="color:#10B981;font-size:0.75rem;margin-left:0.25rem;">
                                                    &#x2191; from <t t-esc="instance.plan_id.cpu_limit"/>
                                                </span>
                                            </t>
                                        </li>
                                        <li>
                                            <i class="fas fa-memory"></i>
                                            <t t-esc="plan.ram_limit"/> RAM
                                        </li>
                                        <li>
                                            <i class="fas fa-hdd"></i>
                                            <t t-esc="int(plan.storage_limit)"/>GB Storage
                                            <t t-if="plan.storage_limit > (instance.plan_id.storage_limit or 0)">
                                                <span style="color:#10B981;font-size:0.75rem;margin-left:0.25rem;">
                                                    &#x2191; from <t t-esc="int(instance.plan_id.storage_limit)"/>GB
                                                </span>
                                            </t>
                                        </li>
                                        <li>
                                            <i class="fas fa-shield-alt"></i>
                                            <t t-esc="plan.max_backups"/> Backups
                                            <t t-if="(instance.plan_id.max_backups or 0) == 0">
                                                <span style="color:#10B981;font-size:0.75rem;margin-left:0.25rem;">NEW</span>
                                            </t>
                                        </li>
                                        <t t-if="plan.recommended_users">
                                            <li><i class="fas fa-users"></i>Up to <t t-esc="plan.recommended_users"/> Users</li>
                                        </t>
                                    </ul>

                                    <!-- Billing period choice + submit -->
                                    <t t-if="plan.yearly_price and plan.yearly_price > 0">
                                        <div class="mb-3">
                                            <div class="small fw-semibold mb-2" style="color:var(--co-text-secondary,#A1A1AA);">Choose billing period:</div>
                                            <!-- Monthly option -->
                                            <form t-attf-action="/my/instances/#{instance.id}/subscribe" method="post" class="mb-2">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                <input type="hidden" name="plan_id" t-att-value="plan.id"/>
                                                <input type="hidden" name="billing_period" value="monthly"/>
                                                <button type="submit" class="btn btn-outline-light w-100 text-start p-3"
                                                        style="border-color:var(--border-color,#27272A);">
                                                    <div class="d-flex justify-content-between align-items-center">
                                                        <div>
                                                            <div class="fw-semibold"><i class="fas fa-calendar me-2"></i>Monthly</div>
                                                            <div class="small mt-1" style="color:var(--co-text-muted,#71717A);">Pay month-to-month, cancel anytime</div>
                                                        </div>
                                                        <div class="text-end">
                                                            <div class="fw-bold" style="font-size:1.1rem;"><t t-esc="'${:,.2f}'.format(plan.price)"/></div>
                                                            <div class="small" style="color:var(--co-text-muted,#71717A);">/month</div>
                                                        </div>
                                                    </div>
                                                </button>
                                            </form>
                                            <!-- Yearly option (highlighted) -->
                                            <form t-attf-action="/my/instances/#{instance.id}/subscribe" method="post">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                <input type="hidden" name="plan_id" t-att-value="plan.id"/>
                                                <input type="hidden" name="billing_period" value="yearly"/>
                                                <button type="submit" class="btn w-100 text-start p-3"
                                                        style="border:2px solid #10B981;background:rgba(16,185,129,0.06);color:var(--co-text-primary,#FAFAFA);">
                                                    <div class="d-flex justify-content-between align-items-center">
                                                        <div>
                                                            <div class="fw-semibold">
                                                                <i class="fas fa-calendar-check me-2" style="color:#10B981;"></i>Yearly
                                                                <t t-if="plan.yearly_discount_pct > 0">
                                                                    <span style="background:#10B981;color:white;font-size:0.7rem;font-weight:700;padding:0.15rem 0.5rem;border-radius:20px;margin-left:6px;">
                                                                        SAVE <t t-esc="'%.0f' % plan.yearly_discount_pct"/>%
                                                                    </span>
                                                                </t>
                                                            </div>
                                                            <div class="small mt-1" style="color:var(--co-text-muted,#71717A);">
                                                                <t t-esc="'${:,.2f}'.format(plan.yearly_price / 12)"/>/mo — Save
                                                                <strong style="color:#10B981;"><t t-esc="'${:,.0f}'.format(plan.price * 12 - plan.yearly_price)"/>/yr</strong>
                                                            </div>
                                                        </div>
                                                        <div class="text-end">
                                                            <div class="fw-bold" style="font-size:1.1rem;"><t t-esc="'${:,.2f}'.format(plan.yearly_price)"/></div>
                                                            <div class="small" style="color:var(--co-text-muted,#71717A);">/year</div>
                                                        </div>
                                                    </div>
                                                </button>
                                            </form>
                                        </div>
                                    </t>
                                    <t t-else="">
                                        <form t-attf-action="/my/instances/#{instance.id}/subscribe" method="post">
                                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                            <input type="hidden" name="plan_id" t-att-value="plan.id"/>
                                            <input type="hidden" name="billing_period" value="monthly"/>
                                            <button type="submit" class="btn btn-primary w-100">
                                                <i class="fas fa-arrow-up me-1"></i>Upgrade to <t t-esc="plan.name"/>
                                            </button>
                                        </form>
                                    </t>
                                </div>
                            </div>
                        </t>
                    </div>

                    <div class="text-center mt-4">
                        <a t-attf-href="/my/instances/#{instance.id}" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left me-2"></i>Back to Instance
                        </a>
                    </div>
                </div>
            </section>
        </t>
    </template>

    <!-- ============================================================
         Checkout  —  /my/instances/<id>/checkout
         ============================================================ -->

    <template id="portal_checkout" name="Checkout">
        <t t-call="saas_website.cloudodoo_layout">
            <section class="py-6" style="padding-top:120px;">
                <div class="container">
                    <div class="mb-4">
                        <h1 class="h3">Complete Your Order</h1>
                        <p class="text-muted">Secure payment powered by SSL encryption</p>
                    </div>

                    <div class="row g-4">
                        <!-- Order Summary -->
                        <div class="col-lg-7">
                            <div class="info-card">
                                <h5>Order Summary</h5>
                                <div class="d-flex align-items-center gap-3 mb-4">
                                    <div class="service-icon" style="width:56px;height:56px;font-size:1.5rem;">
                                        <t t-if="instance.saas_product_id.icon">
                                            <i t-att-class="instance.saas_product_id.icon"/>
                                        </t>
                                        <t t-else=""><i class="fas fa-cubes"/></t>
                                    </div>
                                    <div>
                                        <h5 class="mb-0" t-esc="instance.saas_product_id.name"/>
                                        <span class="text-muted" t-esc="target_plan.name"/>
                                    </div>
                                </div>

                                <!-- Billing period and savings info -->
                                <t t-set="checkout_period" t-value="instance.pending_billing_period or instance.billing_period or 'monthly'"/>
                                <t t-set="checkout_is_yearly" t-value="checkout_period == 'yearly'"/>

                                <t t-if="checkout_is_yearly and target_plan.yearly_discount_pct > 0">
                                    <div class="mb-4 p-3" style="background:rgba(16,185,129,0.08);border:1px solid rgba(16,185,129,0.2);border-radius:10px;">
                                        <div class="d-flex align-items-center mb-2">
                                            <span style="background:#10B981;color:white;font-size:0.7rem;font-weight:700;padding:0.2rem 0.6rem;border-radius:20px;margin-right:0.5rem;">
                                                <i class="fas fa-tag me-1"></i>SAVE <t t-esc="'%.0f' % target_plan.yearly_discount_pct"/>%
                                            </span>
                                            <span style="font-size:0.9rem;font-weight:600;color:#10B981;">Annual billing discount applied!</span>
                                        </div>
                                        <div style="font-size:0.85rem;color:var(--text-secondary,#A1A1AA);">
                                            Regular price: <span style="text-decoration:line-through;"><t t-esc="'${:,.0f}'.format(target_plan.price * 12)"/>/yr</span>
                                            <i class="fas fa-arrow-right mx-2" style="font-size:0.7rem;"></i>
                                            Your price: <strong><t t-esc="'${:,.2f}'.format(target_plan.yearly_price)"/>/yr</strong>
                                        </div>
                                        <div style="font-size:0.9rem;font-weight:600;color:#10B981;margin-top:6px;">
                                            <i class="fas fa-piggy-bank me-1"></i>
                                            You're saving <t t-esc="'${:,.0f}'.format(target_plan.price * 12 - target_plan.yearly_price)"/> per year
                                        </div>
                                    </div>
                                </t>

                                <table class="table table-dark-custom">
                                    <tbody>
                                        <tr>
                                            <td>Instance</td>
                                            <td class="text-end">
                                                <t t-esc="instance.subdomain"/>.<t t-esc="instance.domain_id.name"/>
                                            </td>
                                        </tr>
                                        <tr>
                                            <td>Plan</td>
                                            <td class="text-end" t-esc="target_plan.name"/>
                                        </tr>
                                        <tr>
                                            <td>Billing</td>
                                            <td class="text-end">
                                                <t t-if="checkout_is_yearly">
                                                    Yearly
                                                    <t t-if="target_plan.yearly_discount_pct > 0">
                                                        <span style="background:rgba(16,185,129,0.15);color:#10B981;font-size:0.7rem;font-weight:600;padding:0.1rem 0.4rem;border-radius:12px;margin-left:4px;">
                                                            -<t t-esc="'%.0f' % target_plan.yearly_discount_pct"/>%
                                                        </span>
                                                    </t>
                                                </t>
                                                <t t-else="">Monthly</t>
                                            </td>
                                        </tr>
                                        <tr>
                                            <td>CPU</td>
                                            <td class="text-end"><t t-esc="target_plan.cpu_limit"/> vCPU</td>
                                        </tr>
                                        <tr>
                                            <td>RAM</td>
                                            <td class="text-end"><t t-esc="target_plan.ram_limit"/></td>
                                        </tr>
                                        <tr>
                                            <td>Storage</td>
                                            <td class="text-end"><t t-esc="int(target_plan.storage_limit)"/>GB</td>
                                        </tr>
                                    </tbody>
                                    <tfoot>
                                        <!-- Yearly discount row -->
                                        <t t-if="checkout_is_yearly and target_plan.yearly_discount_pct > 0">
                                            <tr style="color:var(--text-muted,#71717A);">
                                                <td>Regular price</td>
                                                <td class="text-end" style="text-decoration:line-through;">
                                                    <t t-esc="'${:,.2f}'.format(target_plan.price * 12)"/>
                                                </td>
                                            </tr>
                                            <tr style="color:#10B981;">
                                                <td><i class="fas fa-tag me-1"></i>Annual discount (<t t-esc="'%.0f' % target_plan.yearly_discount_pct"/>%)</td>
                                                <td class="text-end">
                                                    -<t t-esc="'${:,.2f}'.format(target_plan.price * 12 - target_plan.yearly_price)"/>
                                                </td>
                                            </tr>
                                        </t>
                                        <!-- Proration credit row (upgrade from existing plan) -->
                                        <t t-if="proration_credit > 0">
                                            <tr style="color:var(--text-muted,#71717A);">
                                                <td>
                                                    <t t-esc="target_plan.name"/> plan
                                                    (<t t-if="checkout_is_yearly">yearly</t><t t-else="">monthly</t>)
                                                </td>
                                                <td class="text-end">
                                                    <t t-esc="'${:,.2f}'.format(target_full_price)"/>
                                                </td>
                                            </tr>
                                            <tr style="color:#10B981;">
                                                <td>
                                                    <i class="fas fa-receipt me-1"></i>
                                                    Credit for <t t-esc="proration_remaining_days"/> unused days
                                                    <t t-if="original_plan_name">
                                                        on <t t-esc="original_plan_name"/>
                                                    </t>
                                                </td>
                                                <td class="text-end">
                                                    -<t t-esc="'${:,.2f}'.format(proration_credit)"/>
                                                </td>
                                            </tr>
                                        </t>
                                        <tr>
                                            <td class="fw-bold fs-5">Total Due Today</td>
                                            <td class="text-end fw-bold fs-5">
                                                <t t-esc="amount" t-options="{'widget': 'monetary', 'display_currency': currency}"/>
                                            </td>
                                        </tr>
                                    </tfoot>
                                </table>
                            </div>
                        </div>

                        <!-- Payment Form -->
                        <div class="col-lg-5">
                            <div class="info-card">
                                <h5>Payment Method</h5>
                                <t t-call="payment.form"/>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </t>
    </template>

    <!-- ============================================================
         Change Plan (Paid to Paid)  —  /my/instances/<id>/change-plan
         ============================================================ -->

    <template id="portal_instance_change_plan" name="Change Plan">
        <t t-call="saas_website.cloudodoo_layout">
            <section class="py-6" style="padding-top:120px;">
                <div class="container">
                    <div class="page-header">
                        <nav aria-label="breadcrumb">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="/my/instances">My Instances</a></li>
                                <li class="breadcrumb-item">
                                    <a t-attf-href="/my/instances/#{instance.id}"
                                       t-esc="instance.subdomain or instance.name"/>
                                </li>
                                <li class="breadcrumb-item active">Change Plan</li>
                            </ol>
                        </nav>
                        <h1>Change Plan</h1>
                        <p style="color:var(--co-text-secondary, #A1A1AA);">
                            Choose the best plan for your needs. Upgrades take effect immediately.
                        </p>
                    </div>

                    <!-- Current plan summary card -->
                    <div class="info-card mb-4" style="border-left:3px solid var(--co-primary, #203c86);">
                        <div class="d-flex flex-wrap align-items-center justify-content-between">
                            <div>
                                <div class="small" style="color:var(--co-text-muted,#71717A);text-transform:uppercase;letter-spacing:0.05em;font-weight:600;">Your Current Plan</div>
                                <div class="d-flex align-items-center gap-3 mt-1">
                                    <h4 class="mb-0" t-esc="current_plan.name"/>
                                    <span class="badge bg-primary"><t t-esc="instance.billing_period or 'monthly'"/></span>
                                </div>
                                <div class="mt-1" style="color:var(--co-text-secondary, #A1A1AA);">
                                    <t t-if="(instance.billing_period or 'monthly') == 'yearly'">
                                        <t t-esc="'${:,.2f}'.format(current_plan.yearly_price)"/>/yr
                                        (<t t-esc="'${:,.2f}'.format(current_plan.yearly_price / 12)"/>/mo)
                                    </t>
                                    <t t-else="">
                                        <t t-esc="'${:,.2f}'.format(current_plan.price)"/>/mo
                                    </t>
                                    <span class="mx-2">|</span>
                                    <t t-esc="current_plan.cpu_limit"/> vCPU
                                    <span class="mx-1">·</span>
                                    <t t-esc="current_plan.ram_limit"/> RAM
                                    <span class="mx-1">·</span>
                                    <t t-esc="int(current_plan.storage_limit)"/>GB Storage
                                </div>
                            </div>
                            <t t-if="instance.next_invoice_date">
                                <div class="text-end mt-2 mt-md-0">
                                    <div class="small" style="color:var(--co-text-muted,#71717A);">Next billing</div>
                                    <div t-esc="instance.next_invoice_date" t-options="{'widget': 'date'}"/>
                                </div>
                            </t>
                        </div>
                    </div>

                    <t t-if="error">
                        <div class="alert-banner alert-danger mb-4">
                            <i class="fas fa-exclamation-circle"></i>
                            <div t-esc="error"/>
                        </div>
                    </t>

                    <!-- Current scheduled change notice -->
                    <t t-if="instance.scheduled_plan_id">
                        <t t-set="cp_is_period_switch" t-value="instance.scheduled_plan_id.id == instance.plan_id.id"/>
                        <div t-attf-class="alert-banner #{'alert-info' if cp_is_period_switch else 'alert-warning'} mb-4">
                            <i t-attf-class="fas #{'fa-sync-alt' if cp_is_period_switch else 'fa-arrow-down'}"></i>
                            <div class="flex-grow-1">
                                <t t-if="cp_is_period_switch">
                                    <strong>Billing Period Change Scheduled</strong>
                                    <p class="mb-2">
                                        Your billing will switch from
                                        <strong t-esc="instance.billing_period or 'monthly'"/> to
                                        <strong t-esc="instance.scheduled_billing_period or 'monthly'"/>
                                        on your next billing cycle
                                        <t t-if="instance.next_invoice_date">
                                            (<t t-esc="instance.next_invoice_date" t-options="{'widget': 'date'}"/>)
                                        </t>.
                                    </p>
                                </t>
                                <t t-else="">
                                    <strong>Downgrade Already Scheduled</strong>
                                    <p class="mb-2">
                                        Your plan will change from
                                        <strong t-esc="instance.plan_id.name"/>
                                        (<t t-esc="instance.billing_period or 'monthly'"/>) to
                                        <strong t-esc="instance.scheduled_plan_id.name"/>
                                        (<t t-esc="instance.scheduled_billing_period or 'monthly'"/>)
                                        at the end of your billing cycle
                                        <t t-if="instance.next_invoice_date">
                                            (<t t-esc="instance.next_invoice_date" t-options="{'widget': 'date'}"/>)
                                        </t>.
                                    </p>
                                </t>
                                <form t-attf-action="/my/instances/#{instance.id}/cancel-downgrade"
                                      method="post" style="display:inline;">
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                    <button type="submit" class="btn btn-sm btn-outline-light"
                                            t-att-data-confirm="'Cancel this billing period change?' if cp_is_period_switch else 'Cancel the scheduled downgrade and keep your current plan?'"
                                            data-confirm-variant="warning"
                                            t-att-data-confirm-yes="'Yes, cancel change' if cp_is_period_switch else 'Yes, cancel downgrade'"
                                            data-confirm-no="No, keep it">
                                        <i class="fas fa-times me-1"></i>
                                        <t t-if="cp_is_period_switch">Cancel Period Change</t>
                                        <t t-else="">Cancel Downgrade</t>
                                    </button>
                                </form>
                            </div>
                        </div>
                    </t>

                    <div class="row g-4">
                        <t t-foreach="plans" t-as="plan">
                            <t t-set="p_monthly" t-value="proration_info.get(plan.id, {}).get('monthly', {})"/>
                            <t t-set="is_current" t-value="plan.id == current_plan.id"/>
                            <t t-set="is_upgrade" t-value="p_monthly.get('is_upgrade', False)"/>
                            <t t-set="cur_period" t-value="instance.billing_period or 'monthly'"/>
                            <div class="col-md-6 col-lg-4">
                                <div t-attf-class="plan-card #{'popular' if is_current else ''}">

                                    <!-- Badge -->
                                    <t t-if="is_current">
                                        <span class="plan-badge" style="background:var(--co-primary, #203c86);">Current Plan</span>
                                    </t>
                                    <t t-elif="is_upgrade">
                                        <span class="plan-badge" style="background:#10B981;">Upgrade</span>
                                    </t>

                                    <h4 class="plan-name" t-esc="plan.name"/>

                                    <!-- Price display -->
                                    <div class="plan-price">
                                        <span class="amount" t-esc="'${:,.2f}'.format(plan.price)"/>
                                        <span class="period">/mo</span>
                                    </div>

                                    <!-- Yearly savings callout -->
                                    <t t-if="plan.yearly_price and plan.yearly_price > 0 and plan.yearly_discount_pct > 0">
                                        <div class="mb-3 p-2" style="background:rgba(16,185,129,0.08);border:1px solid rgba(16,185,129,0.2);border-radius:8px;">
                                            <div style="font-size:0.8rem;color:#10B981;font-weight:600;">
                                                <i class="fas fa-tag me-1"></i>
                                                Save <t t-esc="'%.0f' % plan.yearly_discount_pct"/>% with annual billing
                                            </div>
                                            <div style="font-size:0.8rem;color:var(--co-text-secondary,#A1A1AA);margin-top:2px;">
                                                <t t-esc="'${:,.2f}'.format(plan.yearly_price / 12)"/>/mo billed yearly
                                                <span style="text-decoration:line-through;color:var(--co-text-muted,#71717A);margin-left:4px;">
                                                    <t t-esc="'${:,.0f}'.format(plan.price)"/>/mo
                                                </span>
                                                — Save <strong style="color:#10B981;"><t t-esc="'${:,.0f}'.format(plan.price * 12 - plan.yearly_price)"/>/yr</strong>
                                            </div>
                                        </div>
                                    </t>

                                    <!-- Resources with comparison arrows -->
                                    <ul class="plan-specs">
                                        <li>
                                            <i class="fas fa-microchip"></i><t t-esc="plan.cpu_limit"/> vCPU
                                            <t t-if="not is_current and plan.cpu_limit > current_plan.cpu_limit">
                                                <span style="color:#10B981;font-size:0.75rem;margin-left:4px;">&#x2191;<t t-esc="plan.cpu_limit - current_plan.cpu_limit"/>x more</span>
                                            </t>
                                            <t t-if="not is_current and plan.cpu_limit &lt; current_plan.cpu_limit">
                                                <span style="color:#F59E0B;font-size:0.75rem;margin-left:4px;">&#x2193;</span>
                                            </t>
                                        </li>
                                        <li><i class="fas fa-memory"></i><t t-esc="plan.ram_limit"/> RAM</li>
                                        <li>
                                            <i class="fas fa-hdd"></i><t t-esc="int(plan.storage_limit)"/>GB Storage
                                            <t t-if="not is_current and plan.storage_limit > current_plan.storage_limit">
                                                <span style="color:#10B981;font-size:0.75rem;margin-left:4px;">&#x2191;<t t-esc="int(plan.storage_limit - current_plan.storage_limit)"/>GB more</span>
                                            </t>
                                            <t t-if="not is_current and plan.storage_limit &lt; current_plan.storage_limit">
                                                <span style="color:#F59E0B;font-size:0.75rem;margin-left:4px;">&#x2193;<t t-esc="int(current_plan.storage_limit - plan.storage_limit)"/>GB less</span>
                                            </t>
                                        </li>
                                        <li>
                                            <i class="fas fa-shield-alt"></i><t t-esc="plan.max_backups"/> Backups
                                            <t t-if="not is_current and plan.max_backups > current_plan.max_backups">
                                                <span style="color:#10B981;font-size:0.75rem;margin-left:4px;">&#x2191;</span>
                                            </t>
                                        </li>
                                        <t t-if="plan.recommended_users">
                                            <li><i class="fas fa-users"></i>Up to <t t-esc="plan.recommended_users"/> Users</li>
                                        </t>
                                    </ul>

                                    <!-- What happens + action -->
                                    <t t-if="is_current">
                                        <!-- CURRENT PLAN: billing period switch -->
                                        <t t-if="cur_period == 'monthly' and plan.yearly_price and plan.yearly_price > 0">
                                            <div class="mb-3 p-3" style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;font-size:0.85rem;">
                                                <div class="fw-semibold mb-2" style="color:#10B981;">
                                                    <i class="fas fa-piggy-bank me-1"></i>Switch to Yearly &amp; Save
                                                </div>
                                                <ul class="list-unstyled mb-0" style="color:var(--co-text-secondary, #A1A1AA);">
                                                    <li class="mb-1"><i class="fas fa-check text-success me-2" style="width:14px;"></i>Applied immediately</li>
                                                    <li class="mb-1"><i class="fas fa-receipt me-2" style="width:14px;"></i>
                                                        Remaining monthly credit deducted from yearly price
                                                    </li>
                                                    <li class="mb-1"><i class="fas fa-piggy-bank me-2" style="width:14px;color:#10B981;"></i>
                                                        Save <strong style="color:#10B981;"><t t-esc="'${:,.0f}'.format(plan.price * 12 - plan.yearly_price)"/></strong> per year
                                                    </li>
                                                </ul>
                                            </div>
                                            <form t-attf-action="/my/instances/#{instance.id}/do-change-plan" method="post">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                <input type="hidden" name="plan_id" t-att-value="plan.id"/>
                                                <input type="hidden" name="billing_period" value="yearly"/>
                                                <button type="submit" class="btn btn-success w-100 btn-lg">
                                                    <i class="fas fa-tag me-2"></i>Switch to Yearly — Save <t t-esc="'%.0f' % plan.yearly_discount_pct"/>%
                                                </button>
                                            </form>
                                        </t>
                                        <t t-elif="cur_period == 'yearly'">
                                            <div class="mb-3 p-3" style="background:rgba(255,255,255,0.03);border:1px solid rgba(255,255,255,0.06);border-radius:10px;font-size:0.85rem;">
                                                <div class="fw-semibold mb-2" style="color:var(--co-primary, #203c86);">
                                                    <i class="fas fa-check-circle me-1"></i>Annual Plan Active
                                                </div>
                                                <ul class="list-unstyled mb-0" style="color:var(--co-text-secondary, #A1A1AA);">
                                                    <li class="mb-1"><i class="fas fa-lock me-2" style="width:14px;color:var(--co-warning, #F59E0B);"></i>
                                                        Yearly billing locked until
                                                        <t t-if="instance.next_invoice_date">
                                                            <strong t-esc="instance.next_invoice_date" t-options="{'widget': 'date'}"/>
                                                        </t>
                                                    </li>
                                                    <li class="mb-1"><i class="fas fa-piggy-bank me-2" style="width:14px;color:#10B981;"></i>
                                                        You're saving <strong style="color:#10B981;"><t t-esc="'${:,.0f}'.format(plan.price * 12 - plan.yearly_price)"/>/yr</strong> with annual billing
                                                    </li>
                                                </ul>
                                            </div>
                                            <button class="btn btn-outline-secondary w-100" disabled="" style="opacity:0.5;">
                                                <i class="fas fa-check me-1"></i>Current Plan (Yearly)
                                            </button>
                                        </t>
                                        <t t-else="">
                                            <button class="btn btn-outline-secondary w-100" disabled="">
                                                <i class="fas fa-check me-1"></i>Current Plan
                                            </button>
                                        </t>
                                    </t>

                                    <t t-elif="is_upgrade">
                                        <!-- UPGRADE: immediate -->
                                        <div class="mb-3 p-3" style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;font-size:0.85rem;">
                                            <div class="fw-semibold mb-2" style="color:#10B981;">
                                                <i class="fas fa-bolt me-1"></i>Immediate Upgrade
                                            </div>
                                            <ul class="list-unstyled mb-0" style="color:var(--co-text-secondary, #A1A1AA);">
                                                <li class="mb-1"><i class="fas fa-check text-success me-2" style="width:14px;"></i>Resources upgraded instantly, zero downtime</li>
                                                <t t-if="p_monthly.get('credit', 0) > 0">
                                                    <li class="mb-1">
                                                        <i class="fas fa-receipt me-2" style="width:14px;"></i>
                                                        Credit for <t t-esc="p_monthly.get('remaining_days', 0)"/> unused days:
                                                        <strong style="color:#10B981;">-<t t-esc="'${:,.2f}'.format(p_monthly.get('credit', 0))"/></strong>
                                                    </li>
                                                    <li class="mb-1">
                                                        <i class="fas fa-file-invoice-dollar me-2" style="width:14px;"></i>
                                                        You pay today: <strong><t t-esc="'${:,.2f}'.format(p_monthly.get('final_charge', 0))"/></strong>
                                                    </li>
                                                </t>
                                                <t t-if="p_monthly.get('credit', 0) == 0">
                                                    <li class="mb-1">
                                                        <i class="fas fa-file-invoice-dollar me-2" style="width:14px;"></i>
                                                        You pay: <strong><t t-esc="'${:,.2f}'.format(p_monthly.get('new_price', 0))"/></strong>
                                                    </li>
                                                </t>
                                            </ul>
                                        </div>
                                        <t t-if="plan.yearly_price and plan.yearly_price > 0">
                                            <div class="small fw-semibold mb-2" style="color:var(--co-text-secondary,#A1A1AA);">Choose billing:</div>
                                            <!-- Monthly -->
                                            <form t-attf-action="/my/instances/#{instance.id}/do-change-plan" method="post" class="mb-2">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                <input type="hidden" name="plan_id" t-att-value="plan.id"/>
                                                <input type="hidden" name="billing_period" value="monthly"/>
                                                <button type="submit" class="btn btn-outline-light w-100 text-start p-3"
                                                        style="border-color:var(--border-color,#27272A);">
                                                    <div class="d-flex justify-content-between align-items-center">
                                                        <div>
                                                            <div class="fw-semibold"><i class="fas fa-calendar me-2"></i>Monthly</div>
                                                        </div>
                                                        <div class="fw-bold"><t t-esc="'${:,.2f}'.format(plan.price)"/><span class="small fw-normal">/mo</span></div>
                                                    </div>
                                                </button>
                                            </form>
                                            <!-- Yearly (highlighted) -->
                                            <form t-attf-action="/my/instances/#{instance.id}/do-change-plan" method="post">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                <input type="hidden" name="plan_id" t-att-value="plan.id"/>
                                                <input type="hidden" name="billing_period" value="yearly"/>
                                                <button type="submit" class="btn w-100 text-start p-3"
                                                        style="border:2px solid #10B981;background:rgba(16,185,129,0.06);color:var(--co-text-primary,#FAFAFA);">
                                                    <div class="d-flex justify-content-between align-items-center">
                                                        <div>
                                                            <div class="fw-semibold">
                                                                <i class="fas fa-calendar-check me-2" style="color:#10B981;"></i>Yearly
                                                                <t t-if="plan.yearly_discount_pct > 0">
                                                                    <span style="background:#10B981;color:white;font-size:0.65rem;font-weight:700;padding:0.1rem 0.4rem;border-radius:20px;margin-left:4px;">
                                                                        SAVE <t t-esc="'%.0f' % plan.yearly_discount_pct"/>%
                                                                    </span>
                                                                </t>
                                                            </div>
                                                            <div class="small mt-1" style="color:#10B981;">
                                                                Save <t t-esc="'${:,.0f}'.format(plan.price * 12 - plan.yearly_price)"/>/yr
                                                            </div>
                                                        </div>
                                                        <div class="fw-bold"><t t-esc="'${:,.2f}'.format(plan.yearly_price)"/><span class="small fw-normal">/yr</span></div>
                                                    </div>
                                                </button>
                                            </form>
                                        </t>
                                        <t t-else="">
                                            <form t-attf-action="/my/instances/#{instance.id}/do-change-plan" method="post">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                <input type="hidden" name="plan_id" t-att-value="plan.id"/>
                                                <input type="hidden" name="billing_period" t-att-value="cur_period"/>
                                                <button type="submit" class="btn btn-primary w-100">
                                                    <i class="fas fa-arrow-up me-1"></i>Upgrade to <t t-esc="plan.name"/>
                                                </button>
                                            </form>
                                        </t>
                                    </t>

                                    <t t-else="">
                                        <!-- DOWNGRADE: scheduled -->
                                        <div class="mb-3 p-3" style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;font-size:0.85rem;">
                                            <div class="fw-semibold mb-2" style="color:var(--co-warning, #F59E0B);">
                                                <i class="fas fa-calendar-alt me-1"></i>Scheduled at Cycle End
                                            </div>
                                            <ul class="list-unstyled mb-0" style="color:var(--co-text-secondary, #A1A1AA);">
                                                <li class="mb-1"><i class="fas fa-check me-2" style="width:14px;"></i>No charge now — keep current plan until cycle end</li>
                                                <li class="mb-1"><i class="fas fa-clock me-2" style="width:14px;"></i>
                                                    Takes effect
                                                    <t t-if="instance.next_invoice_date">
                                                        <strong t-esc="instance.next_invoice_date" t-options="{'widget': 'date'}"/>
                                                    </t>
                                                </li>
                                                <li class="mb-1"><i class="fas fa-undo me-2" style="width:14px;"></i>Can be cancelled anytime before</li>
                                                <li class="mb-1"><i class="fas fa-exclamation-triangle me-2" style="width:14px;color:var(--co-warning, #F59E0B);"></i>Resources will be reduced</li>
                                            </ul>
                                        </div>
                                        <form t-attf-action="/my/instances/#{instance.id}/do-change-plan" method="post">
                                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                            <input type="hidden" name="plan_id" t-att-value="plan.id"/>
                                            <input type="hidden" name="billing_period" t-att-value="cur_period"/>
                                            <button type="submit" class="btn btn-outline-secondary w-100"
                                                    data-confirm="This will reduce your resources at the end of the billing cycle. Continue?"
                                                    data-confirm-variant="warning"
                                                    data-confirm-yes="Yes, schedule downgrade"
                                                    data-confirm-no="No, keep current plan">
                                                <i class="fas fa-arrow-down me-1"></i>Schedule Downgrade
                                            </button>
                                        </form>
                                    </t>
                                </div>
                            </div>
                        </t>
                    </div>

                    <div class="text-center mt-4">
                        <a t-attf-href="/my/instances/#{instance.id}" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left me-2"></i>Back to Instance
                        </a>
                    </div>
                </div>
            </section>
        </t>
    </template>

    <!-- ============================================================
         Reactivate Instance  —  /my/instances/<id>/reactivate
         ============================================================ -->

    <template id="portal_instance_reactivate" name="Reactivate Instance">
        <t t-call="saas_website.cloudodoo_layout">
            <section class="py-6" style="padding-top:120px;">
                <div class="container">
                    <div class="page-header">
                        <nav aria-label="breadcrumb">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="/my/instances">My Instances</a></li>
                                <li class="breadcrumb-item">
                                    <a t-attf-href="/my/instances/#{instance.id}"
                                       t-esc="instance.subdomain or instance.name"/>
                                </li>
                                <li class="breadcrumb-item active">Reactivate</li>
                            </ol>
                        </nav>
                        <h1>Reactivate Instance</h1>
                        <p>Reactivate <strong t-esc="instance.subdomain"/> with the same subdomain and domain.</p>
                    </div>

                    <t t-if="error">
                        <div class="alert-banner alert-danger mb-4">
                            <i class="fas fa-exclamation-circle"></i>
                            <div t-esc="error"/>
                        </div>
                    </t>

                    <form t-attf-action="/my/instances/#{instance.id}/do-reactivate" method="post">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                        <div class="text-center mb-4">
                            <label class="form-label">Billing Period</label>
                            <div>
                                <select name="billing_period" class="form-select"
                                        style="width:auto;display:inline-block;">
                                    <option value="monthly">Monthly</option>
                                    <option value="yearly">Yearly</option>
                                </select>
                            </div>
                        </div>

                        <div class="row g-4">
                            <t t-foreach="plans" t-as="plan">
                                <div class="col-md-6 col-lg-4">
                                    <div class="plan-card">
                                        <h4 class="plan-name" t-esc="plan.name"/>
                                        <div class="plan-price">
                                            <span class="amount" t-esc="'${:,.0f}'.format(plan.price)"/>
                                            <span class="period">/mo</span>
                                        </div>
                                        <ul class="plan-specs">
                                            <li><i class="fas fa-microchip"></i><t t-esc="plan.cpu_limit"/> vCPU</li>
                                            <li><i class="fas fa-memory"></i><t t-esc="plan.ram_limit"/> RAM</li>
                                            <li><i class="fas fa-hdd"></i><t t-esc="int(plan.storage_limit)"/>GB Storage</li>
                                            <li><i class="fas fa-shield-alt"></i><t t-esc="plan.max_backups"/> Backups</li>
                                        </ul>
                                        <button type="submit" name="plan_id" t-att-value="plan.id"
                                                class="btn btn-primary w-100">
                                            Select Plan
                                        </button>
                                    </div>
                                </div>
                            </t>
                        </div>
                    </form>

                    <div class="text-center mt-4">
                        <a t-attf-href="/my/instances/#{instance.id}" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left me-2"></i>Back to Instance
                        </a>
                    </div>
                </div>
            </section>
        </t>
    </template>

</odoo>
